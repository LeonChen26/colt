node_modules/
out/
dist/
release/
# 打包试验目录（electron-builder 换 output 时的产物，正式产物仍在 release/）
release-*/
*.log
.DS_Store
.vite/

# 冒烟测试产物
.smoke-*.png

# 助手私有记忆（会话级沉淀，不进版本库；项目约定在 AGENTS.md，那个要提交）
# `.colt/` 与更早的 `.workbuddy/` **两个名字都用过**，两条规则都留着——
# 改名时漏掉一条，旧目录就会静默进版本库，这正是这行注释存在的理由。
.workbuddy/
.colt/

# Logo 的设计工具源文件与导出素材（本地设计产物，不进版本库）
# 按**具体路径**忽略，不用 *.miora / *_assets 这类通配——
# 以后真有个需要提交的同名目录时，通配会静默把它挡在门外。
colt-logo.miora
colt-logo_assets/
