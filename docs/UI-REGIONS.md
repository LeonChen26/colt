# Colt 界面区域定义（现状真源）

> **作用**：约束后续 UI 修改——任何新增 / 移动功能，先在此找到归属区域，再动手。
> **定位**：开发者自用的桌面 Agent 工作台，Work 模式。
> **边界**：只定义**结构职责**，不定义视觉细节。
> **读法**：判断现状**只看 §一~§五**；§六 是版本表，只作追溯。落地细节（文件名 / 阈值 / 锚点）随规则写在文中。
> 相邻文档：跨进程契约 → `ARCHITECTURE.md`；安全边界 → `SECURITY.md`；失败如何呈现 → `ERRORS.md`；计划 → `NEXT-PHASE.md`。

---

## 零、总原则

1. **一个区域只回答一类疑问。** 功能回答的疑问与所在区域不符时，应当搬家，不是就地妥协。
2. **区域按「视角」分层。** 全局 / 会话级 / 运行态 / 外部世界——不同视角的信息不混放。
3. **只读区不承载操作，操作区不承载观测。** 最容易被破坏、最需反复检查的一条。

---

## 一、骨架与视角

```
┌─────────────────────────────────────────────────────────────┐
│ ① 标题栏                    视角：系统（非产品设计）          │
├──────────┬──────────────────────────────┬───────────────────┤
│          │ ② 会话头    视角：会话级      │                   │
│ ③ 左栏   ├──────────────────────────────┤ ⑦ 右栏            │
│ 视角：   │ ④ 会话流    视角：会话级/时间 │ 视角：外部世界     │
│ 全局     ├──────────────────────────────┤                   │
│          │ ⑤ 输入区    视角：会话级      │                   │
│          ├──────────────────────────────┤                   │
│          │ ⑥ 状态栏    视角：运行态      │                   │
└──────────┴──────────────────────────────┴───────────────────┘
```

**结构概括**：**导航 | 叙事 | 现场**——左栏是**入口**（去哪、做什么），中栏是**主线**（你和 agent 的过程），右栏是**副产物**（agent 干活时外界的样子）。

---

## 二、区域定义与规则

### ① 标题栏 · 系统级

| 项 | 内容 |
|---|---|
| **回答** | 我在哪个应用、能否操作窗口 |
| **承载** | 侧栏开关、搜索、菜单、窗口按钮 |
| **禁止** | 任何产品功能 |

- **规则 ①-A**：这里的一切属于操作系统 / Electron 层面，设计决策不在此处做。
- 现状不一致（主题切换、「改动」、「设置」在此）见 §四 #1。

---

### ② 会话头 · 会话级标识

| 项 | 内容 |
|---|---|
| **回答** | 我在哪个任务里？哪些操作是对**这个会话**本身的？ |
| **承载** | 任务标题、会话级操作（分享、复制）、会话上下文（cwd / 分支）、会话级显示入口（统计 / 规则 / 搜索 / 只看问答） |
| **禁止** | **运行态操作**（停止 / 暂停）、**发送参数**（模型、权限模式） |

- **规则 ②-A**：判据是「这个操作在会话不运行时还成立吗？」成立（分享、复制）→ 属这里；不成立（停止）→ 属 ⑤。
- **规则 ②-B**：会话**上下文**（工作目录、分支）必须在此可见。它显示**这条会话自己的**工作目录（由 `SessionInfo.projectId` 反查项目 rootPath），不是「当前选中的项目」——两者只在切项目的那个 IPC 窗口里短暂错开，取后者会显示一个这条会话根本不用的目录（v1.46 收成单一真源）。
- **现状**：cwd + git 分支 chip（游离 HEAD 转琥珀、窗口获焦刷新）；**入口只剩「统计」「规则」两个**——v1.30 由「改动 / 用量 / 工具 / 规则」四收敛为二（「改动」改由 ⑦ 总账进下钻，「工具」并入「统计」）。另有两个**会话级显示入口**：「**搜索**」（原「目录」；v1.45 起目录改由 ④ 左缘点链承担，浮层只留搜索）与「**只看问答**」（整轮折叠，见 ④-E）。它们与「统计 / 规则」不同——不打开右栏、只调本会话的呈现，但同样满足本区域判据。
- **例外**：会话头保留的「压缩上下文」按钮（上下文 >70% 才出现）与 ⑤ 的 `/compact` 是**刻意的双入口**（一处预警式、一处命令式）——见 §四 #2。

---

### ③ 左栏 · 全局导航

| 项 | 内容 |
|---|---|
| **回答** | 我能去哪？我有哪些任务？ |
| **承载** | 模式切换、功能入口、项目 / 任务列表、用户区 |
| **禁止** | 会话内的任何状态 |

- **规则 ③-A**：L1（模式：Work / Code / Design）与 L2（导航：新建任务 / 插件市场 / 模板库 / 自动化 / 办公助理）语义不同，**不可同权重并列**。**状态（2026-09）**：L1 / L2 均为计划中功能、尚未实现，本规则**暂无适用对象**；当前左栏是「项目 → 会话」两层列表——**只有这两层**（v1.66 起下半区的「会话分支树」面板已删，分支能力移进 ④，规则 ④-K），待功能落地时再执行本规则。
- **规则 ③-B**：任务列表是**跨项目、跨会话**的，属全局视角，不随当前会话变化。
- **规则 ③-C**（v1.46）：**侧栏只列已落库的会话**。「还没用起来」的草稿不在其中——判据是「这条会话真的存在过吗」：`session.list` 只读库，而草稿（首次发消息才落库）读不到，故「点了新建又没发消息就退出」不留痕迹。反向的时效要求同样算在这里：首次发消息落库后要**立刻**出现在侧栏——由主进程的**进程状态推送**触发一次列表刷新（草稿落库先于拉起 worker，「有了进程」等价于「已落库」），**不依赖模型是否回话**（模型连接失败也必须看得见）。离开时丢草稿走 `session.discardDraft`（只在仍是草稿时生效），不用 `session.delete`——后者会真的删库。**列表按项目懒加载，且「展开项目」必须触发拉取**（v1.81）：此前只有「把它选成当前项目」（切项目那个 effect）会拉，于是展开一个没选中的项目只会照空缓存画成「还没有会话」——库里全是会话（用户报的就是「除第一个目录外都空着」）。同时把 `undefined`（还没拉到 →「加载中…」）与空数组（确实没有 →「还没有会话」）分开，别再对前者说假话。
- **规则 ③-D**（v1.62）：**移除工作区 = 断开登记 + 清该项目名下的全部会话**，**不碰磁盘上的项目目录**。入口是项目行悬停出的「移除工作区」（垃圾桶，与「+ 新建会话」同排），走 `project.delete`：删项目行、它名下全部会话与派生数据（用量 / 工具 / 改动 / 基线 / 事件 / 审批审计 / 待办）、以及各自的 JSONL 历史，全程一个事务。三条边界：① **注销登记 ≠ 删代码**——目录还在的话重新「打开」就回到原样（`upsertProject` 按 root_key 去重复用同一条登记），但**会话记录不可恢复**，确认框里要如实写明；这也是它与首启「清空重来」（`clearUserData`，连库带目录一起 `rmSync`）的分界——那个是「全都不留」，这个是「只断开这一个」。② **运行中（含正等人授权 / 作答）的会话让整次移除被拒绝**——与 `session.delete` 同一条纪律，且检查放在**任何删除之前**，拒绝时库与磁盘都还没动过。③ 该项目下**没落库的草稿**一并丢掉：它没有库行也没有文件，不理它就成了挂在一条不存在的项目下的内存记录。
- **规则 ③-E**（v1.84）：**「新建会话」有两个入口，语义同一**——① 项目行悬停出的「+」（悬停才显形）：目标是**该项目**，`projectId` 由调用方**显式传入**（防「先切项目再同步建会话」读到本次渲染的旧闭包）；② `SidebarSection` 之上、侧栏最顶部那行**常驻的整行按钮**：没有项目上下文，目标取**当前选中的项目** `activeProject`（与「左栏高亮 / 展开谁」共用同一条规则）。两者建的都是**草稿**（只分配 id、不落库、不进侧栏，见 ③-C），且点击都先 `setMainView("chat")` 回到对话视图。**不合并成一个**：项目行那个能精确指定建到哪个项目，顶部那个是为了「多项目时不必先找对那一行」。一个项目都没有时顶部按钮**置灰**（不是点了没反应）。
- **现状**：运行中会话的**绿点与计时挂在会话行上**（`App.tsx` 的 `SessionRow`），**不在别处**。**v1.66**：左栏下半区那棵「会话分支树」（`features/BranchTree.tsx`）已删——分支的唯一界面入口改成 ④ 每条回复下面那一行按钮（规则 ④-K）。

---

### ④ 会话流 · 叙事主线（核心区域）

| 项 | 内容 |
|---|---|
| **回答** | 我们聊了什么？它做了什么？ |
| **承载** | 思考轨、用户消息、Agent 回复、**工具卡**（内嵌 diff）、授权卡 |
| **顺序** | **严格按时间线性排列** |
| **禁止** | 非本次会话的信息、跨会话汇总 |

**必须承载的信息类型**（每类一个组件）：

| 类型 | 组件 | 来源 | 优先级 |
|---|---|---|---|
| 推理过程 | 思考轨 | `ThinkingRail` / `ThoughtBlock` | P1 |
| 正式回复 | Markdown | `Markdown` | P0 |
| **工具调用** | **工具卡** | `ToolCard` | **P0（最高频）** |
| 子代理委派 | **子代理卡**（`ToolCard` 的特化形态） | `MessageList.tsx` 的 `ToolCard` + `SubagentPreview.tsx` | P1 |
| 权限阻塞 | 授权卡 | `ApprovalCard` | P0 |
| 用户提问（阻塞） | **提问卡**（多题时一题一页、可前后翻；每题除选项外可自由输入，两者合并回传） | `QuestionCard` | P0 |
| 文件变更 | **工具卡内嵌 diff** | `MessageList.tsx` 的 `ToolCard` | P1 |
| **技能调用** | **技能卡**（折叠，展开看模型实际收到的正文） | `MessageList.tsx` 的用户消息分支（`data-conv-skill`） | P1 |

> **订正**：此处原写「变更卡 / `ProjectChanges`」。`ProjectChanges` 实为**主区整页**（项目级、跨会话汇总，由 ① 的「改动」打开），**不在 ④**（④ 禁止跨会话信息）。④ 里承担「文件变更」的是**工具卡内嵌 diff**，它也是「点文件路径 → 下钻预览」的入口。

- **规则 ④-A**：**信息密度必须可调**。工具卡默认折叠成一行（图标 + 名称 + 路径 / 命令 + 增删 + 耗时 + 状态），展开才显示 diff / 输出 / 截图。依据：自用场景要「扫一眼知道发生了什么」。
- **规则 ④-B**：**叙事顺序不可打乱**。推理在结论前、工具调用在结果前、授权卡在它阻塞的那个时刻。
- **规则 ④-C**：工具卡是**出现频率最高**的元素（每轮多个），其设计质量直接决定「能否看懂它在干嘛」。**不可省略、不可简化成一行纯文本**。
- **规则 ④-D**：工具卡图标必须**语义化**（read / edit / bash / browser / computer 各不同），不可统一用扳手。
- **规则 ④-E**（2026-09）：「**只看问答**」。④-A 只管到「逐卡」；一轮十几个调用就是十几行，回看时正文仍被中间步骤淹掉。故把一轮的**过程**（思考 + 全部工具卡）收成一行、点开再展开，只留提问与最终回复（分组见 `lib/turn-groups.ts`，纯函数有单测；渲染在 `MessageList.tsx` 的 `MessageWindow`）。三条硬约束：① 入口在 **②**（会话级显示选项），**默认关**——它改变的是「每次打开看到什么」，而工具卡是叙事的一部分（④-C），不该由产品替所有会话决定收起来；② 收起的那一行**必须是可展开的入口**（「不可省略」不等于「可以删掉」）；③ 判据是**折叠前后提问与最终回复一条不少**（冒烟 `perf` 按行标记逐条核）。
  - **窗口单位跟着折叠走**：不折叠时数**条**消息，折叠时数**轮**——折叠后一轮只占两三行，继续按条数会把窗口吃光，而点这个开关的人要的恰恰是**多读几轮问答**。窗口起点**按单位各记一份**：进折叠时把「条」换算成「轮」（`turn-groups.ts` 的 `turnOfMessage`），展开时把「条」那份**原样还回去**（关掉开关看到的就是打开之前那一屏）。算术见 `lib/message-window.ts` 的 `FOLD_CHUNK`（有单测），两份起点在 `MessageList.tsx` 的 `heads`。
- **规则 ④-F**（2026-09）：**跳到某一轮必须落成「浮动段」**。消息窗口原只有两态——跟最新、或从某点**一直挂到末尾**；用后者跳会一次挂出「目标 → 末尾」几千条，正是窗口要防的事。第三档是**只挂目标那一小段**，上下各能翻页；**翻到底要交回「跟随底部」**，否则之后流式追加的新消息会全落在窗口外，表现为「明明已在最新处，界面却不再更新」。算术见 `lib/message-window.ts` 的浮动段一节（有单测），冒烟判据取「跳过去之后**挂载数仍是一个窗口**、且尾行**不等于**末尾」。
- **规则 ④-G**（v1.45）：**轮次点链挂在 ④ 左缘，是「会话目录」的常驻化**（`TurnRail.tsx`）。此前目录是 ② 浮层里的一半，点开浮层才能跳轮；v1.45 起收成左缘的**点列**——离开顶部才浮现、默认可见 11 个点、轮次更多时 rail 自己滚（`overscroll-contain`，滚到头不带动会话滚动，滚动条隐藏）、悬停展示第 N 轮 + 提问摘要、当前点跟着会话滚动走。两条语义边界：① 点链是**目录**不是分支树——只有一条线性点列、没有分叉；点击走 `history.jumpTo`（**只读定位**，落成 ④-F 的浮动段），**绝不做** `session.navigate`（那是 ④-K 的分叉语义，会改会话本身）。② 数据全在渲染层（`outlineOf` / `turnAt`，有单测），零新 IPC。冒烟判据在 `perf` 模式「六」段。
- **规则 ④-H**（v1.49）：**子代理的卡是工具卡的特化，不是新组件。** `subagent` 是模型的一次工具调用，所以它天然属于 ④ 的工具卡（④-C：不可省略）；特化只加三样——图标换 `Bot`、标题写成「**子代理 · <定义名>**」、状态标签单独标（`data-subagent-card-status`）。展开区里给**有界预览**（`SubagentPreview.tsx`：最近 N 步 + 流式文本 + 思考 + 运行中的工具 + 统计），并**如实说明截断**（「最近 12 步（共 20 步）」——静默砍掉会让用户以为它只跑了 12 步）；**顶部**给「**在右栏查看完整过程**」这个下钻出口——入口排在预览**之前**：预览动辄十来步，排在末尾等于把唯一的深看出口埋掉（冒烟按 DOM 顺序钉住）。**卡随 transcript 长生**：子代理跑完了卡还在（那是这次委派发生过的事实），只是卡上的运行态标记转成完成、卡上的「中止」随之消失（v1.53 前那是「此刻段」那一行的事）。**默认收起**（v1.60 起）：与普通工具卡「运行中默认展开」相反，子代理卡**一律默认折叠**——它的展开区（有界预览 + 深看出口）动辄十来步，跑起来时摊开会把主对话顶得很长；用户需要时自己点开，运行态/完成态由卡面的状态徽标表达，收起不丢信息。展开状态仍归 ④ 的共享表 `toolOpenState`，用户点过就记住。
- **规则 ④-I**（v1.54）：**技能调用不冒充用户发言**。内核把技能正文包成**一条 `role: "user"` 消息**（正文是 `<skill name="…" location="…">…</skill>` 原始 XML），但它**不是用户说的话**——署名不能是「你」，整篇正文也不该摊在对话流里。判据取 `ViewMessage.skill`（worker 投影时用 `parseSkillInvocation` 认出内核 `formatSkillInvocation` 的**固定开头两行**，**不是**渲染层再拿字符串去猜），命中则画成左对齐的可折叠**技能卡**：卡面写「技能 X · 本会话装载，非你的发言」+ 那半句额外指示，正文默认收起、`data-conv-skill-toggle` 展开——**展开是「归属透明」的出口**（用户有权看到模型实际收到了什么）。② 会话目录与**分支树节点**的标签 / 摘要改用单一真源 `skillInvocationLabel`（仍是「技能 X[· 指示]」），不再出现原始 XML。它**仍算一轮**（用户确实敲了这条命令、它也确实换来一次回复），被改的只是**归属**。正常用户气泡补 `data-conv-user` 作对照——冒烟据「有技能卡且无用户气泡」判改归属真的发生。
- **规则 ④-J**（v1.56）：**模型「自己想起来用技能」必须留下痕迹**。模型不会「调用技能」——它读了技能描述后自觉去 `read` 技能文件（内核没有 skill 工具）；不标记的话，界面上只会多一张普通 `read` 卡，看不出「这次读文件是技能驱动的」——而这恰是最该被观测的一件事（它决定模型怎么干活）。故命中「本次 `read` 的路径 = 某已装载技能的文件」时，工具卡上打一个「**技能 X**」徽标（`data-tool-skill`）。两条边界：① 判定在 **worker 投影时**做（技能路径集合就在它内存里，`skillPathMatcher`），渲染层**不重算**路径规则——两处各算一套必然漂；② 徽标是**追加**的，不改工具卡既有形态（④-C 不可省略、④-D 图标语义化照旧）。
- **规则 ④-K**（v1.66）：**分支的唯一界面入口在 ④**——每条回复**下面一行**「从这里分叉」（`data-conv-branch`，`MessageList.tsx` 的 `branchRow`）。此前分支只看得见、只点得到**左栏那棵树**（`features/BranchTree.tsx`，v1.66 已删）：而人想分叉的时刻恰恰是**读着某一轮回复**的时候，跑去左栏找节点是错位的。五条边界：① **落点是尖端就不给**——指针已在尖端，从它分叉等于原地不动。判据是「按钮的落点（该轮最终回复的 id）**是不是尖端那条**」，**不是**「轮 key 是不是末轮的 key」：窗口只挂 50 条，末轮比窗口高时切片从它**中间**开始，`groupTurns` 会以一条助手消息开轮，「末轮 key」那个判据必然为假、假入口照样冒出来（`dock` 冒烟用 61 步的末轮钉着这一条）；② 按钮贴在**该轮最终回复之下**（`turn-groups.ts` 的 `final`，即「最终输出」），点它把**那条消息的 id**（= 内核条目 id，`ViewMessage.id`）交给 `session.navigate`——之后提问落成新分支、原有记录不动；③ **始终可见**，不做 hover 才浮现：它是这一版分支的**唯一出口**，藏起来等于没有；④ **没有回看旧分支的界面入口**——分叉后离开的那条仍在**内核与 JSONL** 里，但 `session.branches` 这条能力**保留而无界面消费者**（见 §四 第 3 条），所以按钮文案必须写「之后的提问会形成新分支」而不是「回到这里」，否则就是在暗示一条不存在的回路；⑤ **跑着的时候点不动**（置灰，不隐藏）——worker 的命令入口是 `void handle(command)`，**不排队**（`entry.ts` 末尾），而 `navigate` 要挪历史指针 + 重拍快照 + 推一次视图；与在飞的那一轮交叠，指针和视图会被那一轮边跑边覆盖。置灰而非隐藏：隐藏会让这一行在每次运行时跳进跳出，而它只是**暂时**不可用（原因写在 `title` 里）。判据取 ⑤ 输入区同一份 `view.running`（**不另算一套**）。⚠️ **残留窗口**：`view.running` 由 worker 推送，渲染层可能晚一拍——理论上「刚落幕、界面还没更新」那一瞬点下去仍会发出去；worker 侧**没有**运行中门（它没有任何 running 状态），所以这一层只是渲染层拦截。<br>另有一条**归属**：这一行是**界面控件**、不是 agent 说的话，所以走 `AssistantRow` 的 `label={null}`（列宽照留、左缘仍与正文列对齐）——顶着「Agent」署名读起来像 agent 在提议分叉。判据在两处：`dock` 冒烟（受控视图推两轮：非末轮恰一条按钮、位置在最终回复之后 / 下一轮提问之前、整行文本就是按钮文案即**不署名「Agent」**、点击送出的是最终回复 id 而**渲染时不发**、视图收成一轮后按钮消失、**运行中置灰且标题说明原因 / 跑完恢复可点**；**外加末轮比窗口还高的切法**——先证前提「该轮提问行没挂载、尖端已挂载」，再断言不给按钮）；**worker 侧那一段**（拒绝子 lane 的节点、先挪指针后重拍快照、异常不吞）由 `tests/navigate.test.ts` 覆盖——它原来是 `entry.ts` 里没法单测的一小段，v1.66 抽成 `worker/lib/navigate.ts` 的 `applyNavigate`。

---

### ⑤ 输入区 · 交办

| 项 | 内容 |
|---|---|
| **回答** | 我要它干什么？**用什么参数**？ |
| **承载** | 输入框、附件、**模型选择**、发送 / 停止 |
| **禁止** | 运行状态观测（那属于 ⑥） |

- **规则 ⑤-A**：判据是「这是**发送这条消息的参数**吗？」是（模型）→ 属这里；不是（上下文占用、心跳）→ 属 ⑥。
- **规则 ⑤-B**：**停止 / 中断**属于这里。理由：它是「控制本次运行」的操作，与「发起本次运行」（发送）同源。
- **规则 ⑤-C**：窄宽度时**先折叠文字标签，再考虑换行**。发送按钮永不收缩、永不被挤出。
- **规则 ⑤-D**（2026-09 修订）：**权限（审批）模式在此区域呈现**——它属于「发送这条消息的参数」（同 ⑤-A），位置是输入工具行右侧的下拉。历史：曾写作「暂不在此区域呈现」，后由实现确定归属。
- **规则 ⑤-E**（v1.44）：**斜杠命令的候选浮层**属于这里，且**必须是浮层、不占工具行的常驻宽度**。理由有二：一是 ⑤-C 已规定工具行在窄宽度下的行为，而那一行已有 4 个控件、还带着一条「卡片不能被裁」的回归，再挂一个常驻控件等于把问题推给它；二是命令的**名字本来就要打在输入框里**，提示贴着输入框长才自然。硬约束：**浮层只在候选真的存在时才出现**——否则用户每次贴 `/usr/local/bin` 这类路径都会被一个空菜单打扰。
- **规则 ⑤-F**（2026-09）：**斜杠命令共三条，分两类语义**，判据全在 `lib/slash-command.ts`（纯函数、有单测）。
  - **零参数**：`/compact`（压缩上下文）、`/memory-tidy`（整理沉淀的记忆）——必须**独占整条输入**（`"/compact"` 或带尾空格都算，`"/compact 一下"` 不算），**不做前缀匹配**；否则用户想发「用 /compact 手动压缩」这句正文会被当成命令吞掉（v1.34 的教训）。
  - **带参数**：`/skill <名字> [额外指示]`——只能前缀匹配，故加两道阀限制代价：命令字须**整段等于** `skill`（`/skills`、`/skillfoo` 都不是命令），且后面须真有**非空名字段**。
  - 名字不存在时**不回落成普通消息**，而是**就地拦下**并给出同一句话（文案在 `@shared/skill-error`，渲染层与 worker 共用同一份）——「敲了没反应」比报错更伤信任。
  - 名单**来源**是本会话的 `ConversationView.skills`（v1.53 起是整份 `ViewSkill[]`，渲染层只取 `name`）。⚠️ 拿不到视图时它是 `undefined` ＝ **不知道**，必须**放行**——当「空的」会把一次有效调用误判成失败，那是另一种丢输入。
- **规则 ⑤-G**（v1.46）：**没有会话也要能交办**。项目在跟前、却一个会话都没有时，输入区**照常在场**（为此自动建一条**草稿**：只分配 id，不落库、不 fork worker、不建 JSONL）。理由：输入区是「交办」的唯一入口，把它藏在「先新建会话」后面，第一屏就只剩一块**只能看的空屏**。自动建草稿同时满足 ③-C，因此「见得到输入框」与「侧栏不留空会话」两件事可以同时成立。反向约束：中间区**不再出现「新建一个会话开始对话」这类指路文案**——入口本身就是输入框。
  - 判据是纯函数 `shouldOfferDraft`（`lib/session.ts`，有单测）：三件事必须分开——列表还没到（`undefined`）要**等**、有落库会话就不必给、当前已是本项目的一条（含草稿）就不能再给（否则会无限建）。
- **规则 ⑤-H**（v1.47）：**起手态把「提示块 + 输入卡片」一起上移到相对中间**，并且**只有起手态才提供改目录的入口**。
  - **排布**：没有任何消息时，左列换成「会话头 / 0 / 1fr」——输入列拿到第 3 行的整块高度，用 `justify-center` 把两块**一起**居中（`StartPanel.tsx` 是提示块，`Conversation/index.tsx` 的输入列是输入卡片；两块在**同一列里上下相邻**，不是父子）。有消息后回到「消息流占满、输入区贴底」：贴底是**交办**的常态。起手态的判据是 `empty`（没消息 + 没在启动 + 没报错 + **没有待办提示**）——最后一项不能省：「还差一步配密钥」那条黄条住在消息区里，而起手态要把消息区收起来，不排除它就会把黄条一起藏掉，等于**静默失败**（见 `ERRORS.md`）；宁可这时不显示起手提示块。
  - **输入卡片的宽度由「可用宽度」决定，不由内容撑**（v1.60 修）：外层是 `mx-auto w-full max-w-[796px] px-[18px]`——**`w-full` 不能省**。起手态的输入列是 flex 列，`mx-auto` 的跨轴 `auto` 外边距会打断 flex 的 stretch，少了它卡片就按内容收缩（实测中栏 642px 时只有 445px，比它上面的提示块还窄）。成立时宽度 = `min(796, 中栏宽) − 36`，与消息列（`max-w-3xl`）基本对齐；这条有冒烟哨兵（拖右栏把中栏压到 360 后按公式逐像素核）。
  - **起手态的空输入框固定 2.5 行高**（v1.60 定 3 行；v1.72 随字号上调把 73px 调到 81px；**v1.83 按用户反馈下调到 69px / 2.5 行**）：`min-h-[69px]` ＝ 2.5×行高(22.75) ＋ 上下内边距(12)。**同批给非起手态定 2 行基线**（`min-h-[54px]`＝2×行高＋内边距 8——原先只有 1 行约 31px，用户反馈「高一点」）；两态都只是下限，多行输入仍由 `resizeInput` 按内容长到 180 上限。⚠️ 该高度只在**行高没变**时才等于行数：起手态往 `textarea` 上额外挂字号类会让 tailwind-merge 把 `leading-relaxed` 当冲突项**静默删掉**，行高退回继承来的 1.5，同一个 69px 就不再是 2.5 行（v1.60 事故的触发写法是当时的 `text-[13px]`；现行 `text-sm` 与 `leading-relaxed` 合并正常，冒烧行高断言守着）。冒烟用两条断言守：**行数 = 2.5**、**行高 = 1.625×字号**（都按元素自己的计算样式现算，不写死像素）。
  - **目录只在起手态可改**：`<工作目录> + [换一个目录] [新建工作目录]`，后两个是**图标按钮**（`FolderOpen` / `FolderPlus`，见 v1.60）。发完第一条消息目录就定死了（`project_id` / JSONL 目录 / worker 的 cwd 全按它，换目录等于换会话），所以不做成常驻控件；平时要看目录的地方是 ② 会话头。
  - **「换一个目录」= 换项目**（`project.pick`）：`sessions` 表**故意不存 cwd**，「读项目内文件」的根与记忆隔离键（`normalizeRootKey`）都由项目根推出——会话级 cwd 会一次牵动三处消费方，按换项目实现是零成本的。
  - **「新建工作目录」= 主进程造一个**（`project.createScratch`）：`~/.colt/<年月日-时分秒>/workspace`——父目录沿用仓库既有的**用户级命名空间**（用户级记忆就是 `~/.colt/memory.md`，见 `worker/lib/memory.ts`），家目录里只开这一个口子；时间戳精确到**秒**（两次不同时刻的意图必须拿到**两个**目录——只到分钟时一分钟内点两次会落到同一个已装东西的目录，「给我一块干净的地方」当场落空；同一**秒**内重复点才落到同一路径、由 `upsertProject` 按 root_key 去重）。最里层的 `workspace/` 才是**项目根**（名字固定，时间戳那层留给这一次的日志与产物）。落在**家目录**（而非应用数据目录）是因为它是**用户的工作产物**，必须找得到。`COLT_WORKSPACE_ROOT` 非空时原样使用（冒烟靠它钉在 `out/smoke-workspace`）。
  - **目录属于「项目」，不属于「会话」**：多条会话可共用同一工作目录（同一项目下点「+」开的第二条会话与第一条同目录）；「新建工作目录 / 换一个目录」换的是**当前这条草稿**的归属（换项目），别的会话原地不动。
  - **分支只显示、不切检出**：`git.ts` 只读 `.git/HEAD`，仓库里没有任何 git 写能力；「选分支」等于新引入「检出」这条写路径，故先不做——要换分支就交给 agent（它有 bash）。② 会话头的分支徽标照旧只读。
  - 锚点：`data-conv-start` / `data-conv-workdir` / `data-conv-newdir`；另 `data-conv-card`（输入卡片，探针按它认，别用 `parentElement`）、`data-conv-session`。

---

### ⑥ 状态栏 · 运行态观测

| 项 | 内容 |
|---|---|
| **回答** | 现在什么情况？**它还活着吗**？ |
| **承载** | 上下文占用、成本、心跳（已耗时 + 最后活动）、stale 警告 |
| **禁止** | **任何操作**（按钮、开关）、发送参数 |

- **规则 ⑥-A**：**只读区不放操作。** 本区域最容易被破坏的规则。反例：曾有中断按钮、模型选择器、会话级模式选择器置于此，均已移除。
- **规则 ⑥-B**：**必须每秒走字**。心跳的意义在于「数字在动 = 它活着」；停止走字本身就是信号。
- **规则 ⑥-C**：**超时不许伪造活动**。超过阈值无事件时**如实**提示，**不可用转圈动画假装在忙**。实现口径：心跳常显「最后活动 Ns 前」；超过 `STALE_IDLE_SEC = 30` 秒无事件时该读数转琥珀，并追加「似乎卡住了，可中断」。提示的是「没动静」这件事本身，不把它改写成「还在忙」，也不给用户一个假的进度。
- **规则 ⑥-D**（2026-09 落定）：运行态之外**必须给出明确的状态**，不留空。状态段由 `running` + 最近一轮终态（`ConversationView.lastRun`）推出，共四态：运行中（绿点脉动）/ **已中断**（灰点 + 文案）/ **已失败**（红点 + 文案 + 一行截断的 error 摘要，悬停看全文）/ **空闲**（灰点 + 「空闲」）。
  - 只有**中断**与**失败**单独留一行：正常跑完（`completed`）与「还没跑过」一样回到「空闲」——状态条不该为一次正常的结束留痕。判定是纯函数 `lib/format.ts` 的 `runStateOf`（有单测）。
  - 终态取内核 `LaneSnapshot.lastResult` 且**只认 `kind === "run"`**（压缩 / 导航的终态不答「我交办的事」这一问）。**别用 `faulted`**：那是 harness `fault` 事件的会话级硬故障、内核从不复位它（该字段已从 `ConversationView` 删除，见 `ERRORS.md`）。
  - 状态是**电平**：结束态一直留到下一轮跑完，不是一闪而过的提示。颜色只是**辅助**，每个态旁边都有文字（「颜色必须配文字」）。
  - 已知限制：`lastResult` 是 lane 生命期的记录，worker 被空闲回收后重建（切会话 / 重启）会丢 → 回落成「空闲」。
- **规则 ⑥-E**：位置在**输入区下方**。理由：与「交办」动作相邻，发完即可观察状态。
- **规则 ⑥-F**（v1.70 补）：**整条居中**。依据是高保真稿写死的那条 `.live-bar { justify-content: center }`。补这条是因为它**真的被改歪过**：v1.64 那次「对齐与节奏」把它改成 `justify-start`（理由「与左对齐的输入卡同一条基线」），而 A~E 里一条都不管对齐——**没有规则的地方最容易被顺手改掉**。注意它与卡片的分工：卡片自己的文字靠 `px-4` 内缩，状态条**不跟着卡片内缩**，两者居中对齐的是**同一个容器的宽度**。

---

### ⑦ 右栏 · 工作区（Workspace）

| 项 | 内容 |
|---|---|
| **回答** | Agent 正在操作的**对象**长什么样？以及我想看的任何附属内容 |
| **定位** | **一个可插拔的容器**，不是某个专用栏 |
| **默认视图** | **任务摘要**（计划进度 + 此刻在跑什么 + 本次改动总账） |
| **其他视图** | 浏览器、阅读器、文件……**不限个数** |
| **宽度** | **由用户拖拽决定**（220px ~ 窗口中栏留 360），视图切换不覆盖 |
| **禁止** | 会话叙事（那属于 ④） |

**定义要点**：右栏**就是工作区**，「任务摘要」只是它的默认视图。

| 视图类型 | 内容 | 对应疑问 |
|---|---|---|
| **任务摘要**（默认） | 计划进度（将来）+ 此刻在跑什么（现在）+ 本次改动总账（过去），后者下钻清单 → diff → 内容 | 这件事进行到哪了 |
| **外部世界** | 浏览器页面、URL、console / network / downloads、用户自己的前进 / 后退 / 刷新、装不下时的「适应宽度」 | 它在网上操作什么 |
| **阅读** | Markdown / 文本阅读器 | 我想看什么内容 |
| **文件** | 系统文件浏览 | 项目里有什么 |
| **附属内容** | 会话统计（原「用量历史」）、审批规则 | 这次会话花了多少、被放行 / 拦了什么 |

#### 代码现状与范围（2026-09）

- **页签现有七个 kind**：**任务摘要（默认，kind 仍为 `follow`）/ 浏览器 / 文件（v1.77）/ 终端（v1.78）/ 统计 / 规则 / 事件**（`WorkspaceDock.tsx` 的 `DOCK_KIND_META`；「事件」是 F3 安全事件流的回查页签，v1.50）。
- **「阅读 / 文件」分两层**：下钻主路径仍是**「点文件路径 → 预览」**（A3-2）：入口是 ④ 工具卡的副标题（**仅当副标题就是该工具操作的文件时**才可点，判定用「展示文本 === 路径」而非「有 `path` 参数」，避免 `grep` / `glob` 的搜索模式被误当文件）；**整项目树 v1.77 起落地为「文件」页签**（只读懒加载树 + 复用 `FilePreview`，见版本表 v1.77；v1.80 起**内容在左、树在右且树可整体收起**）——原 A3「不做整项目树」的决定被 2026-09 的用户需求推翻：定位不是替代 IDE，是现场「项目里有什么」随手可查。
- **预览按类型分流**：`.md` 走 Markdown 渲染；其余文本按扩展名认语言（`renderer/src/lib/code-lang.ts`）后交 highlight.js 着色 + 左列行号，**认不出就原样等宽、不猜**；图片给 dataUrl、二进制与过大只给提示（不做半截渲染）。语言映射只列 common 包里注册过的语言（由单测逐条核对）。
- **预览 / 清单是「任务摘要」的下钻，不各占页签**（v1.32 起，详见规则 ⑦-G）：清单按目录**一层**分组（不是可折叠树）、同一文件折 `×N` 带 `+a −b`，只列 `fileChanges` 并排除越界路径、底部如实计数（约束 ⑦-4）。
- **页签模型是「实例列表 + `activeId`」**（A3-1）；**页签可关闭 + 「+」新增视图**（A3-3）——「可关闭」与「菜单里能打开」是**同一集合**（清单由 `closable` 推导），故结构上不存在「关了回不来」；菜单**只列产品里真有的视图**（现为 `browser / files / terminal / usage / rules / events` 六项；`files` / `terminal` 随 v1.77 / v1.78 入列）。
- **两个观测 / 管理面板已迁入本容器**（A3-5）：「统计 / 规则」由**中栏浮层**改为 ⑦ 的**可关闭页签**——依据正是本栏定义里的「**以及我想看的任何附属内容**」与「其余视图不限个数」。迁入后**只有页签这一个载体**：面板自身不再有「收起」（关闭交给页签的 ×），② 的两个入口与「+」菜单都只是打开同一页签的快捷方式。
- **浏览器页签底部有「观测抽屉」**（B2）：控制台 / 网络 / 下载三份观测。它读主进程 `CaptureBuffer`——与 agent 的 `browser_read` **同一份**缓冲，不存在「界面说的和模型看到的不一样」。抽屉**属于浏览器视图内部**，不另立页签（它回答的是「这个页面怎么了」，脱离页面就没意义，与 ⑦-C 一致）。抽屉占页面区域的高度，收起 / 展开经 ResizeObserver → `browser.bounds` 同步给原生视图。**行是概览，点开才是全貌**（v1.33）：点一行在下方展开「字段表 + 复制」，`URL` / 路径上的原生 tooltip 随之撤掉；字段映射抽在 `lib/observe-detail.ts`（纯函数，有单测），详情段落带自动滚入可视区。
- **浏览器页签顶部有「后退 / 前进 / 刷新」**（B1）：由**用户**发起，与 agent 的 `browser_act` 是两条链路——那条走审批（裁决的是模型给出的入参），这条没有模型参与、没有可裁决的对象，**不走审批**；何况内嵌页是真实 `WebContentsView`，用户本来就能直接点它。按钮可用性来自主进程现读的 `navigationHistory`（渲染层不自己记历史，那必然与真实 webContents 走偏）。页面被换掉后 agent 的判断会过期，故同时**告知 agent**：经 `browserNotice` → worker 在下一次模型请求前用 `transform_context` 注入环境提示（不写进 transcript），且**只在 agent 正在跑时**转发。详见 `NEXT-PHASE.md` B1。
- **原生视图的矩形每 400ms 重申一次**（窗口不可见时暂停，v1.51）：视图必须**一直**等于「页面区域」，而 ResizeObserver / window resize 都是**边沿**触发——漏一次边沿就永久错位（实测：抽屉再展开时页面区域矮了 132px，视图却停在收起时的高度，于是压住观测抽屉）。冒烟按「**逐像素**对齐 + 反复收起 / 展开 5 轮」验。
- **视口联调覆盖会在头部显形**：`browser_act viewport` 设的尺寸是**持久**状态（只在显式「恢复」时撤销），且会让原生视图比停靠区更大（实测覆盖 1280×800、停靠区 823×643 时，右侧 209px 被窗口边缘裁掉、下方 157px 压住观测抽屉）。现在头部显示 `视口 1280×800 · 恢复`（`data-browser-viewport` / `data-browser-viewport-reset`），未被视图盖住的页面区域改用中性底。**「适应宽度」**（v1.40）把整页等比缩小到刚好装下：只改页面 CSS 视口（`setZoomFactor`），**不动原生视图矩形**，故逐像素对齐不受影响；比例钳在 `[MIN_FIT_ZOOM, 1]`（可读下限 60%），顶到下限仍装不下就如实说并**撤掉按钮**；「还原」出口常驻工具条；`contentWidth` **只在 `zoom === 1` 时量**。
- **「终端」页签是用户直操作能力面**（v1.78）：xterm.js（渲染层）+ 主进程 PTY（ConPTY），shell 由主进程挑（pwsh → powershell → cmd）、cwd 钉在会话所属项目根（主进程按 sessionId 推出，同 `file.read` 口径）；生命周期锚主进程——切页签 / 切会话不杀（渲染层卸载只取消订阅，StrictMode 双挂载靠 `open` 幂等消化），收口三层：页签 × / 会话 dispose / app 退出。输出 64KB 环形缓冲（切页签期间靠它回放）+ 16ms 合帧推送。**模型工具面零入口**：不走审批（用户直敲、没有可裁决对象，同浏览器 B1 的用户侧按钮），将来接给 agent 必须作为工具过 `before_tool` 闸门（`SECURITY.md` §五）。详见版本表 v1.78。

- **规则 ⑦-A**：⑦ 是**「现场」**，④ 是**「叙述」**。④ 说「我点了登录按钮」，⑦ 展示那个按钮本身。两者是同一件事的两种表达，**不可互相替代**。
- **规则 ⑦-B**：**⑦ 是工作区容器，不是浏览器专用栏。** 以**页签**切换视图；「任务摘要」是默认视图，其余视图**不限个数**。
  - **宽度只由用户拖拽决定**，且是**全局统一值**——**不随页签变化**。依据：原按视图类型各给建议值（任务摘要 300 / 浏览器 544 …），于是切页签就改宽度，中栏跟着忽宽忽窄、内容反复重排，是纯粹的视觉噪声。现在未拖拽时一律取 `DOCK_DEFAULT_WIDTH = 544`（取各类视图里较宽的那个需求；v1.24 前那条「『文件』双栏树要求 ≥520」的依据已随 v1.32 失效，现依据是**下钻的 diff 与文件预览需要横向空间**）。
  - **代码现状**：拖拽把手**已实现**（`Conversation/index.tsx` 的 `.dock-grip`，9px 命中区浮在两栏分隔线上，双击复位）。宽度取 `用户拖拽值 ?? 统一默认宽度`，并钳制「右栏 ≥ 220px、中栏 ≥ 360px」；窗口缩放时按当前可用宽度重新钳制。拖拽位移只对**位移**取负（`startWidth − dx`），见 `AGENTS.md` 3.3。本规则已完整成立。
  - **「浏览器」按需加载**：未用到时不创建 WebContents（对齐 `browser-host` 的懒创建）。
- **规则 ⑦-C**：**视野跳跃必须为零。** 任何需要用户「在另一个窗口里找」的设计都是错的。依据：外部窗口会破坏 ④ 与 ⑦ 的并置关系。
- **规则 ⑦-D**：⑦ 的更新**不可抢焦**（不改变用户正在读的 ④），仅静默更新自身内容。
- **规则 ⑦-E**：**默认视图必须是「任务摘要」，且不可关闭（但可折叠）。** 理由：自用场景下，「这件事进行到哪了 + 它正在碰哪些文件」是判断安全性的第一依据，必须常驻可见。折叠态收为 44px 图标条（保留页签与活动指示），**不提供完全关闭**。
  - **更名（v1.48）**：原叫「正在处理」，补上计划段后覆盖面成「将来到过去」，故更名**任务摘要**；`DockKind` **仍是 `follow`**（kind 是内部标识）。别处的同名字样（`ChangeDrilldown` 面包屑、冒烟按标签断言的用例）须同批改，否则「页签与面包屑不同名」。
  - **代码现状**：折叠态已实现——收为 44px 图标条；折叠态**不渲染**拖拽把手，且**同步收起原生浏览器视图**（折叠即上报 `null`）。折叠态点图标条上的视图入口 = **切到该视图 + 展开**（只切不展开等于点了没反应）。
  - **「它是活的吗」由 ⑥ 状态栏（⑥-D 四态）+ ④ 的运行中工具卡回答**。原「进行中的动作」段已删（v1.53）：与 ④ 工具卡重复，且 `flex-1` 吃掉剩余高度、把计划挤进 `max-h`、总账顶到面板最下沿。
  - **不可关闭是规则，不靠「碰巧没人调用」保证**：默认视图**不渲染关闭按钮**，`closeDockInstance` 有 `isDockClosable` 守卫。可关闭视图现为 `browser / files / terminal / usage / rules / events`（由 `closable` 推导成「+」菜单项），出口两条：`+` 菜单、⑦-F。
- **规则 ⑦-F**：**agent 开始操作某个视图的对象时，自动切到对应视图。** 具体：agent 首次使用浏览器 → 右栏自动切到「浏览器」页签。**优先级**：此场景下 ⑦-C（视野跳跃为零）**优于** ⑦-D（不抢焦）——agent 开始用浏览器 = **新现场的开始**，必须被看到；用户随时可切回其他页签。
  - **代码现状**：若右栏当时处于折叠态，自动切页签的**同时展开右栏**（只切页签而右栏还收着，等于没被看到）。
- **规则 ⑦-G**：**改动清单不另立视图，它是「任务摘要」的下钻；总账是「任务摘要」的一行。**
  - 「任务摘要」由**几段结论**构成，互不重复（**顺序即阅读顺序**）：

    | 段 | 时态 | 内容 | 独有性 |
    |---|---|---|---|
    | **本次用量**（v1.61 新增，**v1.67 置顶**） | 过去 | 费用 + 输入 / 输出 tokens 的**结论**（取自 `view.stats`，零 IPC）；**无消耗时整段不渲染**；出口「查看完整统计」→ **「统计」页签** | 结论在 ④；**流水只在「统计」** |
    | **计划**（v1.48 新增） | 将来 | 待办清单：`N/M` 进度 + 进行中那条（带 `activeForm`）+ 待做；**已完成折成一行「已完成 N 项」**（点开看） | **全应用唯一出处** |
    | **本次改动（总账）** | 过去 | 一行：`N 处 · M 文件 +a −b`，**紧跟计划**（本视图**最后一段**），点击进入清单 | **全应用唯一出处** |

  - **用量段为什么存在（v1.61）/ 置顶（v1.67）**：计划与改动两段都可能为空（计划无清单整段不渲染、改动没改过只剩「0 处 · 0 文件」），面板显得空；用量段用**结论**填上，数据取视图自带的 `stats`（零 IPC、不把 loading / 错误态引进常驻视图）。**只给结论**：按模型 / 工具排行 / 可筛明细仍归「统计」页签（⑦-H）——「结论在此、流水在彼」，不是把统计面板搬进来。置顶依据：三段里只有它带**钱**（花销比进度更早被关心），且零消耗时整段消失、放最上面不留空壳（重排只换顺序，内容 / 口径 / 空态规则一字未动）。

  - **v1.53 删去「进行中的动作」段**（理由见 ⑦-E 的归位）：此刻动作**只在 ④**；子代理的「中止」随之挪到 **④ 的子代理卡**上，**只在 `status === "running"` 时出现**（已结束的卡上不放点了没反应的按钮）。
  - **本规则真正的不变量是「同一类对象只在一处渲染」，不是字面上的「几段」。** 它当初要消除的是**同一个 `fileChanges` 被渲染三遍**（`FollowPanel` 的改动段 / 汇总行 / `ChangesPanel` 列表）；计划与改动是**两类不同对象**，各画一次故不违背本规则。反过来两条约束仍然有效：① **不得把「计划」再抄一份到别的视图**（② 已决定**不加**常驻进度入口）；② **不得把两段并成一个大列表**——时态会混在一起。
  - **计划段的清单不设自己的滚动上限**：清单多长就多长，超出面板高度由**本视图整体**滚动，不在半截处截断（曾写死 `max-h-[45%]`，清单略多就只在自己那一小块里滚）。
  - **总账不是视图，不另立页签，也不钉在右栏底部**：它属于「任务摘要」，**紧跟计划**、跟着该视图出现、随切页签消失（那一刻用户在别的现场，「这次会话改了多少」不是他要看的）。总账由「可折叠区段」降级为**「一行状态」**（不是 `fsec`，没有 caret / 展开态 / 空态）。
  - **「处 / 文件」两个数都必须显式命名**：总账写「**N 处 · M 文件**」——「处」是改动条数（`fileChanges.length`）、「文件」是去重后的文件数。**两个口径确实不同**：总账用**原始**计数（含项目外条目），清单头部用 `buildChangeList` 的**过滤后**计数（排除项目外）；`±` 只在清单侧算且给的是**净值**（v1.36）。这不是缺陷（各有理由），但**不能再写「一个口径」**。
  - **「任务摘要」→ 清单 → diff → 文件内容是一个东西的连续下钻**，层级用面包屑表达，ESC 逐层回退。用户不需要知道「该去改动页签还是文件页签」，只需要知道「想看得更细」。
  - **下钻目标从「文件内容」多了一种到「子代理流」**（v1.49）：点 ④ 子代理卡的「在右栏查看完整过程」→ 落到**子代理流层**（`SubagentStream.tsx`）。与改动那条线**并排**：不经过清单、回退直接回「任务摘要」。**按状态分两个真源**（v1.52）：运行中走视图通道（有界尾部、并明说不完整）、跑完才换按需拉的完整流（`session.subagentTranscript`，10s 超时；快照静止，用它渲染会「概览比详情新」）；拉不到时**如实说「还没有留下过程记录」**，不白屏、不假装有内容。
  - **代码现状**：①② 在 v1.29 落地（`FollowPanel.tsx`）；③④⑤ 在 v1.32 落地（`ChangeDrilldown.tsx` + `lib/change-list.ts` + `FilePreview.tsx`）——「改动」「文件」页签**整个取消**，总账与 ④ 工具卡路径进同一套下钻：`list`（按目录一层分组、同一文件折 `×N`）→ `diff`（可切历史）→ `content`，出口三条（面包屑 / 层底「返回」/ ESC 逐层回退）。越界过滤收在 `buildChangeList` 的 `isProjectRelative`，清单底部如实写「已隐藏 N 个」。层状态由 `ChangeDrilldown` 自持（容器只管 `drillEntry`），切页签不丢下钻、换会话即退出。
- **规则 ⑦-H**：**附属视图给「结论」，不给「流水」。**
  - 流水在别处已有：**④ 消息流的工具卡就是工具调用的叙事流水，且比面板多一个「输出」**；附属视图的价值在**聚合**，不在罗列。

    | 视图 | 聚合内容 | 明细去处 |
    |---|---|---|
    | **统计** | 总额（tokens / 费用）+ **按模型分组**（次数 / tokens / 费用 / 占比）+ **工具调用次数排行** + **工具耗时排行** + 失败统计 | **并入本视图的折叠明细区**（带筛选） |
    | ~~工具~~ | **已取消独立视图**（v1.31）：聚合与明细都归「统计」 | ④ 消息流的工具卡（看单次调用的最佳位置） |

  - **聚合数据全部可从现有字段算出，不需改协议**：`ToolCallRecord` 已有 `toolName` / `durationMs` / `isError` / `createdAt`；`UsageRecord` 已有 `model` / `inputTokens` / `outputTokens` / `cache*Tokens` / `costUsd`。
  - **本视图名称为「统计」，不用「用量」**：「用量」一词在**收窄自己**（只盖得住费用与 tokens，「工具调用次数」「耗时排行」「失败率」都不是「用量」），名实不副会让用户按名字建立错误预期。
  - **排名必须带分母**（「共 42 次」），否则「排行」只是又一个列表。**明细必须能被筛选，否则不算并入**：从聚合行点进去 → 明细自动筛成该维度，并至少提供「全部 / 只看失败」——这才让并入产生新能力（④ 会话流给不了「只把 bash 挑出来看」）。
  - **不要做实时监控**：这是**事后回顾**视图，数据来自 DB（重启后仍可看），不需要订阅推送 / 轮询。
  - **② 的「改动」「工具」入口已删**（v1.30）：判据是「该视图在别处有没有常驻出口 / 它给的信息别处有没有」——改动有总账（删）、工具有 ④ 工具卡（删）、统计与规则无（留，且「规则」是**有写操作的问题驱动入口**，用户发现「明明没点拒绝却总被拦」时会主动来找，必须永远可见）。
  - **代码现状**：② 的 4 → 2 与改名在 v1.30 落地、面板聚合与「工具」取消在 v1.31 落地；⑦-G 第四步（v1.32）又把「改动」「文件」两个 kind 一并取消（并入下钻），故「+」菜单由 6 项收敛到 **3 项**（`browser / usage / rules`）。聚合规则抽在 `lib/session-stats.ts`（纯函数，有单测）。

#### ⑦ 实现约束（用户不可感知，但代码必须遵守）

- **约束 ⑦-1：「不限个数」指的是页签，不是同时挂载的 WebContentsView。** 各视图资源成本差异极大（任务摘要纯 DOM≈0；浏览器 = 一个独立渲染进程，几十到几百 MB）。因此：页签可以很多（只记录状态与位置）；同时存活的重型视图必须限制；超出的应**挂起**（销毁视图、保留页签与 URL，切回时重建）。**现状：限 2~3 个 / 超限挂起从未实现**（并入 `NEXT-PHASE.md` 的「事 B」），别当成已生效。
- **约束 ⑦-2：懒创建必须到页签层，不只是浏览器。** `browser-host.ts` 已是会话级懒创建；接入工作区后应下沉到**视图级**——未激活的页签不创建 `WebContentsView`。
- **约束 ⑦-3：视图销毁必须可回收。** `browser-host.ts` 注释已记录过这个坑：新窗口一律不开——默认行为会生出一个**不受本类管理**的窗口，读不到、`disposeAll` 也回收不掉，而 agent 后续 `snapshot` / `text` 仍停在旧页面上，**表现为「点了没反应」**。任何视图都必须登记在容器里，能随会话 / 页签关闭或挂起而正确销毁，不留孤儿进程。顺序是「摘出视图 → 判活（destroyed 后访问属性会抛）→ 关」。
- **约束 ⑦-4：读文件视图的「根」只能由主进程推导；路径必须落在根内。** 载体已由「文件」页签变更为「任务摘要」下钻的**内容层**（`panels/FilePreview.tsx`），约束本身不变。渲染项目内文件时，根由主进程按 `sessionId → 项目 → root_path` 查出，并做三层校验（`resolve` 后仍须落在根内 → `realpath` 之后**再判一次** → 大小上限 + 二进制判定）。**绝不能让渲染层指定读哪个根**——渲染层会渲染 agent 生成的 Markdown，一旦放开等于把任意读盘能力交出去。路径**相对 / 绝对皆收**（工具入参里的 path 由模型给出，可能是绝对路径），但都必须落在根内；**越界判断是纯字符串运算，先于任何 fs 访问**——根外的绝对路径不会去碰磁盘，因此不会变成「某路径存不存在」的探测口。实现见 `src/main/file-read.ts`。另注：`sessions` 表**不存 cwd**，根只在 `projects.root_path` 上。

---

## 三、归属判定速查表

新增功能时，按顺序自问：

| 问题 | 归属 |
|---|---|
| 它是操作系统层面的窗口操作吗 | ① |
| 它是**关于会话本身**的操作，且会话不运行时也成立吗 | ② |
| 它是**跨会话 / 跨项目的导航**吗 | ③ |
| 它是**对话过程的一部分**，需按时间出现在叙事里吗 | ④ |
| 它是**发送消息的参数**或**控制本次运行**吗 | ⑤ |
| 它只是**观测**运行状态吗 | ⑥ |
| 它是**agent 动作对象的投影**（页面 / 文件）吗 | ⑦ |

**都不符合 → 不要加**，而不是「找个地方塞进去」。

---

## 四、代码现状 vs 本定义（不一致清单）

> 复核时间：2026-09（第二次复核）。依据 `src/renderer/src/`、`src/main/`。
> **读法**：本表只列**当前仍然存在**的不一致。「已修正」的历史条目不再逐条留在这里——它们连同修正时间都在 §六版本表里。

| # | 区域 | 不一致事实 | 涉及规则 | 处置 |
|---|---|---|---|---|
| 1 | ① | 标题栏里有**产品功能**：主题切换、「改动」（切到主区的项目改动整页）、「设置」 | ①「禁止任何产品功能」 | **待决**：要么改规则（承认顶部是产品导航条），要么把这两个入口挪进 ③ |
| 2 | ② | 会话头有**「压缩上下文」按钮**（上下文 >70% 时才出现） | ②「禁止运行态操作」；同一能力在 ⑤ 还有 `/compact` | 双入口**刻意保留**（一处预警式、一处命令式），已就地补例外（见 §二 ②），免得下一个人照规则把它删掉 |
| 3 | ④ / ③ | **`session.branches` 保留而无界面消费者**：v1.66 把分支的唯一入口改成 ④ 的「从这里分叉」（规则 ④-K）并删掉左栏树面板之后，「列出全部分支」这条能力（`session.branches` ↔ worker `branches` ↔ `projectBranchNodes`）在 **`src/main` + `src/renderer` 里再没有任何调用点**（`src/dev/smoke` 的 `advanced` 仍直接打它——那是开发期装置，不是应用） | 意图如此（**保留能力、去掉界面**） | **有意保留**：分叉后离开的那条分支仍在内核与 JSONL 里，这条通道是将来「回看旧分支」唯一的现成底座。两条禁令——**不要**因为它没人调用就顺手删掉，也**不要**反过来以为它还在界面上（那是「只有定义、没有调用」的反向误判） |

**已关闭的旧条目**（勿再按它们判断现状）：

- ④ 类型表曾把「变更卡 / `ProjectChanges`」列为 ④ 的组件 → **已就地改注**：④ 里承担文件变更的是**工具卡内嵌 diff**。
- ⑤-D 原写「权限模式暂不在此区域呈现」→ **已就地改注**（规则没错，错的是那句状态；现在它就在 ⑤）。
- ⑥ 曾含模型选择器 / 会话级模式选择器、且位于输入区**上方** → **已修正**，⑥ 现为**只读区**（⑥-A）；模式选择器现在 ⑤。
- ② 曾缺 git 分支 → **已修正**（`git.status`，含游离 HEAD 警示 + 获焦刷新）。
- ④ 工具卡曾缺失 → **已修正**。
- ⑦ 曾是跟随线独占、浏览器曾是独立窗口 → **均已修正**（页签工作区 + 内嵌 `WebContentsView`）。
- ③ 的「L1 / L2 同权重并列」：**规则暂无适用对象**——那两个 UI 从未存在（见 §二 ③）。

---

## 五、待决问题

> **只留仍然待决的。** 已完成的不再各占一行——结论都已写进对应规则与 §六版本表。保留编号，是为了不让历史引用（`NEXT-PHASE.md` 等处）失锚。
> **下一阶段的计划与优先级不在本表**——`NEXT-PHASE.md` §3.1 原 N1…N4 计划已于 2026-09 删除、暂空；前置不具备的见其 §3.2。

| # | 问题 | 影响区域 | 状态 |
|---|---|---|---|
| 7 | **agent 与「多开浏览器实例」的绑定**：`browser_act` / `browser_read` 作用于哪个实例 | ⑦ | **待决**（「事 B」的前置，见 `NEXT-PHASE.md` §3.2） |

**已关闭**：

- ~~1 模式切换（L1）与功能入口（L2）视觉未分层~~ → **无适用对象**：那两个 UI 从未实现（§二 ③）。
- ~~2 会话上下文（cwd / 分支）未在 ② 展示~~ → **已完成**（含游离 HEAD 提示 + 获焦刷新）。
- ~~3 工具卡在设计稿中缺失~~ → **已完成**。
- ~~4 ⑦ 的多视图切换规则未定义（浏览器 ↔ 跟随线）~~ → **已完成**（页签切换；宽度为**全局统一 544**，见 ⑦-B）。
- ~~5 ⑥ 空闲态的呈现未设计~~ → **已完成**（C1 + C2 四态：运行中 / 已中断 / 已失败 / 空闲）。
- ~~6 权限模式的归属（发送参数 vs 会话属性）~~ → **已落地在 ⑤**。
- ~~8 ⑦ 的页签是**固定 2 个**还是**可开实例**~~ → **已完成**（实例模型 +「+」菜单与页签关闭；**同 kind 多实例**并入「事 B」）。

---

## 六、版本表

> **只作追溯**。判断现状只看 §一~§五。已被后续版本推翻的都在「现状 / 取代」列标出。

### 已被取代的旧状态一览（免得逐条翻）

| 旧状态 | 取代者 |
|---|---|
| ⑦ 按视图类型取建议宽度（任务摘要 300 / 浏览器 544 / 文件 560 / 改动 420 …） | v1.24 → 全局统一 **544** |
| `dockWidth` 按**激活实例的 kind** 取建议值 | v1.24 |
| 容器查询四档（760 / 560 / 520 / 400） | 后续扩为**六档**（900 / 760 / 620 / 560 / 520 / 400） |
| 「文件」视图（`DockKind` 扩为 `follow｜browser｜file`） | v1.32 → 取消，改为下钻**内容层** |
| 点工具卡路径「**激活『文件』页签**」 | v1.32 → 落进下钻内容层，不切页签 |
| 「文件」视图 = 预览左 + 200px 改动树（`lib/file-tree.ts`） | v1.32 → `lib/change-list.ts`（`file-tree.ts` 已删） |
| `DockKind` 七个 kind；「+」菜单 6 项 | v1.31 / v1.32 → **四个** kind、**3** 项 |
| 「改动」「工具」两个 kind 仍可从「+」菜单打开 | v1.32 → 两个 kind 都已删 |
| 总账出口**暂时**落在「改动」页签 | v1.32 → 落到**清单层** |
| `UsagePanel` 内部标题仍写「用量历史」 | v1.31 → 「会话统计」 |
| 重型视图限 2~3 个 / 超限挂起（v1.6） | **从未实现**（并入「事 B」）——别当成已生效 |
| 「跟随线（follow line）」可收成右侧边缘标签 / 浮层 | **该组件在本产品里不存在**——它就是 ⑦ 的默认页签（现称「任务摘要」） |

### 版本

| 版本 | 变更 | 现状 / 取代 |
|---|---|---|
| v1 – v1.2 | 首次落定；补 §四 违规清单（权限模式标「暂不处理」）；④ 工具卡入稿（5 形态） | — |
| v1.3 | ⑦ 改页签多视图容器（任务摘要 300 / 浏览器 544） | 按视图取值已被 v1.24 取代 |
| v1.4 / v1.10 | ⑦ 折叠态（44px 图标条）：保留页签与活动指示、**不提供完全关闭**；折叠即收起原生视图；⑦-F 折叠态下同时自动展开 | 现行 |
| v1.5 | ⑦ 重定义为**工作区**，其余视图不限个数；宽度只由拖拽决定 | 「仅未拖过时用建议值」已被 v1.24 取代 |
| v1.6 | 实现约束：页签不限个数，重型视图（WebContentsView）限 2~3、超出挂起 | **从未实现**（并入「事 B」） |
| v1.7 | ② 补 git 分支（`git.status` 读 `.git/HEAD`）；⑥/⑤ 容器查询窄栏降级 | 断点后扩为六档 |
| v1.8 | ⑦ 内嵌浏览器：独立 `BrowserWindow` → `WebContentsView`，新增 `browser.bounds` / `browser.state`，独立 partition，首次加载自动切页签（⑦-F） | 现行 |
| v1.9 | ⑦ 拖拽把手 `.dock-grip`（9px 命中 / 1px 指示线 / 净占位 0），钳制右栏 ≥220 / 中栏 ≥360 | 现行 |
| v1.11 | 视图范围决定：**不做整项目树**、浏览器多开后置、协议迁移独立成批（仅记决定） | 现行 |
| v1.12 | A3-1 页签实例模型（`DockKind` + `DockInstance`），⑦-F 改 `ensureDockInstance` | 按激活 kind 取宽已被 v1.24 取代 |
| v1.13 / v1.14 | A3-2 文件预览（`file.read`）：相对 / 绝对皆收但须在根内；入口含工具卡副标题（判据「展示文本 === 路径」） | 「文件」页签已被 v1.32 取消（改为下钻内容层） |
| v1.15 | A3-3 页签关闭 + 「+」新增（菜单由 `closable` 推导）；**菜单只列真有的视图** | 菜单 6 项已被 v1.32 取代（现 3 项） |
| v1.16 | A3-4 内容树（范围 A：只列 `fileChanges`）；必须排除越界路径 | 已被 v1.32 取代（`file-tree.ts` 已删） |
| v1.17 | A3-5 四个中栏面板（改动 / 用量 / 工具 / 规则）迁入 ⑦ 成可关闭页签；面板自身不再有「收起」 | 七 kind / 菜单 6 项已被 v1.31、v1.32 取代 |
| v1.18 | B2 观测抽屉（控制台 / 网络 / 下载）挂在浏览器页签内，1s 轮询 | 现行 |
| v1.19 | B1 前进 / 后退 / 刷新：用户操作**不走审批**但告知 agent（`browserNotice` → `transform_context`） | 现行 |
| v1.20 – v1.23 | 原生视图**逐像素对齐**（电平状态 vs 边沿触发）、视口覆盖「可见 + 恢复」标记、「右边显示不全」归因订正（窄栏页面最小宽 768 vs 覆盖两条来源） | 现行 |
| v1.24 | 宽度改**全局统一 544**（不再按视图取建议值） | 现行 |
| v1.25 | C1+C2：⑥ 四态（运行中 / 已中断 / 已失败 / 空闲），抽 `runStateOf` | 现行 |
| v1.26 | 计划重新基线化（仅文档）：宽度表述统一、指针指向 `NEXT-PHASE` §3.1 / §3.2 | 现行 |
| v1.27 / v1.28 | 规则 ⑦-G（总账 + 下钻，数字「N 处 · M 文件」）/ ⑦-H（附属视图给结论不给流水；「用量」改名「统计」、删「工具」、会话头 4→2）（仅文档 + 概念稿） | 现行 |
| v1.29 | ⑦-G 第一步：段一不再列已完成文件；底部「本次改动」降为**一行常驻总账**（双口径，空闲时仍在、不给出口） | 出口「暂时落在改动页签」已被 v1.32 取代 |
| v1.30 | ⑦-H 第二步：**会话头 4→2**（删「改动」「工具」）；页签标签「用量」→「统计」、图标换柱状图 | 「删入口不等于删能力」已被 v1.31 / v1.32 推翻 |
| v1.31 | ⑦-H 第三步：`UsagePanel` 重写为 KPI + 按模型 + 工具次数 / 耗时排行 + 可筛明细；**`tools` kind 整体移除**（菜单 6→5） | 菜单 5 项已被 v1.32 取代（现 3 项） |
| v1.32 | ⑦-G 第四步：**「改动」「文件」两个页签整个取消**，成为「任务摘要」的下钻（`ChangeDrilldown` 清单 → diff → 内容；`change-list.ts` 取代 `file-tree.ts`；越界过滤收口到 `isProjectRelative`）；`DockKind` 只剩 4 个、菜单 5→3 | 现行 |
| v1.33 | 观测抽屉**条目详情**（原 N1 ①）：点行展开字段表 + 复制、撤原生 tooltip、自动滚入可视区、一次只展开一条 | 现行（时间戳 / 耗时 / 请求头仍缺，未排期） |
| v1.34 | `/compact` 斜杠命令：手动压缩上下文（**加入口**，压缩链路早已存在）；命令识别为纯前端、整串相等、未知放行；顺带修 worker 回收自愈 `compactOrReconnect` | 现行 |
| v1.35 | 全库缺陷审计：逐条交叉验证后批量修复（工具卡展开态丢失 / 自动滚底抢滚动 / 导航清缓冲时机 / RPC 超时 …） | 现行 |
| v1.36 | ⑦ 的「本次改动」显示**净值**：多次改动看「最终改成了什么」；改回原样写**已还原**；**算不出不显示数字**并如实计数；diff 层多一档「累计」（基线 → 当前）。根因：此前只存「每次改了什么」、从没存「改之前是什么」 | 现行 |
| v1.37 | 文件内容**按格式渲染**：`.md` 之外认扩展名后交 highlight.js 着色 + 行号，**认不出不猜**（`code-lang.ts` / `CodeView.tsx`） | 现行 |
| v1.38 | 思考等级成为**会话配置**（默认「高」），修掉「/compact 点了没反应」——根因是我们自己发了 `thinking:disabled`，压缩那条**不带工具**的请求被模型拒 | 现行 |
| v1.39 | 三条「说不出来」的体验：**等待授权**（任务栏闪 + 系统通知 + 侧栏「等待你的授权」）/ **附件静默**（被跳过的贴卡片内提示）/ **页面装不下**（量 `contentWidth` 算出来再提示，不是「栏一窄就报警」） | 现行 |
| v1.40 | **「适应宽度」**：装不下的页面等比缩小（只改页面 CSS 视口、不动视图矩形）；比例钳 `[60%, 1]`；「还原」常驻工具条；`contentWidth` 只在 `zoom===1` 时量 | 现行 |
| v1.41 | `dock` 的 `/compact` 段改为**只记账、不转发**——此前三个桩转调真实现，把测试文本当成真 prompt 发了出去（真计费 + 写进用户真实会话历史） | 现行 |
| v1.42 | `/skill` 斜杠命令：把「按名调用技能」变成用户够得着的能力（新增 IPC `session.skill`） | 「先清空再发」导致额外指示被吃掉，已由 v1.43 修 |
| v1.43 | 打错技能名时**不再吃掉那半句额外指示**：新增 `ConversationView.skills`，判定**前移到渲染层**（提交当刻判成败才能「拦下时不清空输入」）；worker 那条降级为兜底；文案移到 `shared/skill-error.ts` 两边共用 | 现行 |
| v1.44 | 「**/ 候选浮层**」：`slashCandidates` + `absolute bottom-full`，选中写回输入框（技能项带尾随空格）；匹配规则的前缀**且不等于自己**（敲全后浮层让开）；浮层开着时 Enter 是「选中」 | 现行 |
| v1.45 | **轮次点链**：会话目录从 ② 浮层收成 ④ 左缘的常驻导航（`TurnRail.tsx`，规则 ④-G）；② 会话头「目录」改名「搜索」，浮层只留搜索 | 现行 |
| v1.46 | 「没用过的会话」的界面规矩：**打开就见输入框**（自动草稿，规则 ⑤-G）但**侧栏不留空会话**（规则 ③-C）；cwd 改为由**会话自己的项目**推出（规则 ②-B）；新增 `session.discardDraft` | 现行 |
| v1.47 | **起手区**（规则 ⑤-H）：提示块 + 输入卡片一起上移到相对中间；目录只在起手态可改（换目录 = 换项目 `project.pick`；新建工作目录 = `project.createScratch` 生成 `~/.colt/<秒级时间戳>/workspace`）；分支只显示不切检出 | 现行 |
| v1.48 | ⑦ 默认视图升为「**任务摘要**」：更名（`DockKind` 仍是 `follow`）+ 三段（计划 → 此刻 → 改动总账）；**② 不加常驻进度入口** | 现行（`FollowPanel.tsx`；数据来自 `ConversationView.todos`，无新 IPC） |
| v1.49 | **子代理呈现**（规则 ④-H）：④ 特化出**子代理卡**（`Bot` 图标 + 有界预览 + 下钻出口）；下钻多一种目标**子代理流**（按需拉 `session.subagentTranscript`）；不自动展开右栏（不抢焦）；阻塞卡加「来自 <子代理名>」chip | 现行（`SubagentPreview.tsx` / `panels/SubagentStream.tsx`；数据来自 `ConversationView.subagents`） |
| v1.50 | **「事件」页签**（F3 的界面半）：安全类 notice（技能装载告警 / 同名覆盖 / 读取失败）落 `session_events` 表，右栏新增可关闭页签做持久回查（toast 消失后仍可查）；kind 变 5 个、「+」菜单 3→4 项 | 现行（`DOCK_KIND_META.events` / `session.events.list`） |
| v1.51 | **周期定时器「窗口不可见就停表」（F11）**：所有服务「用户正在看」的周期任务（⑥ 心跳、侧栏计时、观测抽屉轮询、视图 400ms 重申、倒计时）统一走 `lib/visible-interval.ts`——不可见即停，重新可见立即补跳一次再续周期 | 现行（`useVisibleInterval`；逐拍单测 `visible-interval.test.ts`；冒烟前提「窗口钉为可见」——遮挡下停表是产品行为） |
| v1.52 | **子代理下钻按状态分真源**（v1.49 的完善）：**运行中**走视图通道（实时刷新、只有有界最近几步、并在那一层**明说**）、**跑完之后**才换 `subagentTranscript` 拉回的完整流——快照拉回来就静止，用它渲染会造成「概览（④ 卡）比详情新」；④ 卡深看出口**移到预览之前**（不被预览埋掉） | 现行（`SubagentStream.tsx` 的 `LiveStream` / `FetchedStream`） |
| v1.53 | **技能装载的「状态」与「事件」分开**（P4）：`ConversationView.skills` 从 `string[]` 升为整份 `ViewSkill[]`——**装载状态由视图承载**，不再靠 `notice` 播报（例行「已加载 N 个技能」会把真告警挤掉）；`describeSkills` 收窄为 `describeSkillWarnings`，只报遮蔽 / 解析失败这类**事件** | 现行（`ViewSkill` / `toViewSkills`） |
| v1.54 | **技能调用不再冒充用户发言**（P2，规则 ④-I）：内核把技能正文包成**一条 `user` 消息**，原按用户发言处理（④ 用户气泡、② 原始 XML 标签）。现 `ViewMessage` 加 `skill` 标记（投影时 `parseSkillInvocation` 认出），④ 改画**可折叠技能卡**，② / ③ 标签改用单一真源 `skillInvocationLabel` | 现行（`@shared/skill-invocation`；`data-conv-skill`） |
| v1.55 | **技能进设置页**（P1 / P5 / A2）：设置页新增「技能」分区（名字 / 说明 / **出处如实标** / 是否对模型公开 / 路径；零技能给放置引导；项目级技能补**来源提醒**——它会进系统提示词）；新增 `skills.status` / `skills.rescan`，重扫经 `harness.setResources` 热更新、当轮生效不必重启。⚠️ 右栏（⑦）**不做**技能面板（只显示当前会话用到的技能，见 P3 工具卡标记） | 现行（`Settings.tsx` 的 `SkillSettings`；`skills-command.ts`） |
| v1.56 | **技能工具卡标记**（P3，规则 ④-J）：模型不「调用技能」——它自觉去 `read` 技能文件，原本只会多一张普通 `read` 卡。投影时判定「`read` 路径命中某已装载技能的文件」（`skillPathMatcher`：绝对路径比对、相对先按 `cwd` 解析、目录内其它文件不算），命中打「技能 X」徽标 | 现行（`skillOfReadCall`；`data-tool-skill`） |
| v1.57 | **技能「查看正文」+ 正文长度上限**（P6 / P7）：① 设置页技能卡可展开看 SKILL.md 全文——正文会进系统提示词、用户有权看到；正文**只进按需查询**（`skills.status`），**不进**每 50ms 推送的 `ConversationView`。② 给**模型**那份超 `MAX_SKILL_BODY_CHARS`（8000）即**截断 + 指回文件**；装载结果不截断（用户看全文）、超限如实进告警 | 现行（`capSkillBodies` / `toSkillDetails`；`data-skill-toggle`） |
| v1.58 | **技能单个启用 / 禁用 + 「在文件管理器中显示」**（P6 写的那半）：开关关掉 = **完全不装载**（不进提示词、`/` 候选也不列——敲了必被拒的名字不摆进候选），`/skill <被禁名>` 回「已被你禁用」。偏好只写**项目级** `.colt/skills.json`；两层**并集**语义——用户级 `~/.colt/skills.json` 的禁用是全局的，项目级**不能**启回去（开关置灰 `disabledByUser`）。**不做删除**，替代出口「在文件管理器中显示」（`skills.reveal`，只认磁盘 `.agents/skills/<名>/SKILL.md` 形状、文件不在如实回） | 现行（`@shared/skills-config`；`data-skill-switch`） |
| v1.59 | **内置技能（随应用分发）+ 正文规模如实标注**：目录两层变**三层**（项目 / 用户 / 内置——内置随安装包分发、**优先级最低**、磁盘同名可盖，与内置子代理同口径）。界面：① 来源标新增「内置」档（`SKILL_SOURCE_LABEL` 是 `Record`，加档会编译不过）；② 技能卡如实报**正文规模**（`data-skill-size`，`N 行 · M 字符`）；③ **内置技能不画「在文件管理器中显示」**——app.asar 里够不到，画了就是死控件（**不放宽校验**：放开到任意路径等于路径枚举口子，与 `SECURITY.md` §三 冲突）。遮蔽告警按 `dirSources` 如实说「X 覆盖了同名 Y」（三层后原固定文案会说假话）；系统提示词补技能纪律（「放进去即装」「落地前**通读**全文」）并明说**不提供技能的下载 / 搜索 / 安装接口** | 现行（`worker/lib/builtin-skills/`；`isSkillFilePath`） |
| v1.60 | 起手/空态视觉重排（规则 ⑤-H 的呈现层，规则本身没改）：输入卡片补 `w-full`（外层缺它 + `mx-auto` 的跨轴 auto 外边距打断 flex stretch，导致卡片按内容收缩）；起手态输入框固定 3 行高（⚠️ 3 行高在 v1.72 随字号上调从 `min-h-[73px]` 调到 `min-h-[81px]`；⚠️ 起手态不要在 `textarea` 上覆盖字号，会让 tailwind-merge 静默删掉 `leading-relaxed`）；提示块理层次；目录两入口换成图标按钮。**正常态一字不动**（差异全由 `empty` 门住） | 现行（`StartPanel.tsx` / `Conversation/index.tsx`；冒烟 `model` 的 `[session/draft]`） |
| v1.62 | **移除工作区**（规则 ③-D）：项目行新增入口 + IPC `project.delete`——断开登记并清该项目名下**全部会话**（记录 + 派生数据 + JSONL 历史），**磁盘目录一行不动**；运行中（含等人回话）的会话让整次移除被拒绝（检查排在**任何删除之前**） | 现行（`ipc/index.ts` / `db/repo.ts` 的 `deleteProject` / `App.tsx` 的 `ProjectRow`；冒烟 `dock` + `rm-workspace`） |
| v1.63 | 视觉精修（令牌层 + 观感），**不动结构、不动区域规则**：新增 `--color-line-soft` / 阴影令牌 / `--radius-xs` / 过渡曲线，补 `::selection` 与 `:focus-visible`；修左栏 `hover` 同色无反馈；通知条改中性描边 + 左侧色条（v1.71 去掉色条）；会话行加 `tabular-nums` | 现行（`styles.css` 等；无断言依赖这些样式） |
| v1.64 | **度量统一 + 对齐与节奏**：圆角 / 字号 / 字距 / 语义色收敛成一套词表（具名字号 `text-xs/sm→11.5/13` 等）；新增 `--h-panel-head` 并接上死掉的 `--h-topbar`；修三处窄栏观感缺陷；git chip 由死按钮降为纯展示 `<span>`。**⚠️ 其中「状态栏改 `justify-start`」在 v1.70 已改回居中**（那条改动无规则依据、且与高保真稿相反） | 现行（`styles.css` / `App.tsx` / `Conversation/index.tsx` 等；无断言依赖这些度量） |
| v1.65 | **提问卡：多题翻页 + 每题自由输入**（`ask_user` 呈现层，**不改协议**）：一次只渲染一题（页码 + 上/下题，单题不出翻页控件，翻页保留已选与已填）；每题常驻自由输入框，与选项**合并**回传（`composeAnswer` / `isAnswered`，纯函数有单测）；回车＝非末页翻页 / 末页提交，**`isComposing` 为真必须放行**（中文输入法确认候选词）；**空问卷早退成 `null`** 不白屏 | 现行（`QuestionCard.tsx` + `renderer/src/lib/question-answer.ts`；冒烟 `ask-user` 39/39） |
| v1.66 | **分支能力从 ③ 迁进 ④**（规则 ④-K）：删左栏「会话分支树」面板（`features/BranchTree.tsx`），改在 ④ 每条回复下面一行「从这里分叉」（`data-conv-branch`）——落点是尖端就不给、始终可见、**没有回看旧分支的界面入口**（`session.branches` 保留而无消费者）、跑着时置灰；该行不署名「Agent」 | 现行（`MessageList.tsx` / `Conversation/index.tsx` / `App.tsx`；`navigate.test.ts` 6 条；冒烟 `dock` 246/246） |
| v1.67 | **「任务摘要」三段重排**：顺序改为 **本次用量 → 计划 → 本次改动**（内容 / 口径 / 空态规则一字未动，只换顺序与相邻分隔线）。依据：用量是唯一带**钱**的结论，且零消耗时整段不渲染。顺序写成 DOM 序断言（`dock`） | 现行（`FollowPanel.tsx`） |
| v1.68 | **修两处工具卡缺陷**：① 卡片宽过它那一列（副标题 `min-w-0 flex-1` 让固有宽度＝整条路径）→ 卡片根元素加 **`max-w-full`**；② 路径被展开按钮顶到行中间（按钮 `flex-1` 与副标题均分余量）→ 展开按钮**不伸缩**。两处都写成几何断言（须**现造长路径**，短路径是必然为真的假阴性） | 现行（`MessageList.tsx`；`dock` +3） |
| v1.69 | **长串文本的横条：把 ④ 会话区那条不变量做成通例**（v1.68 只修了一个实例）：技能卡正文 / 用户气泡 / 思考块 / `SubagentPreview` 四处原样文本统一收口，判据是「会话区 `scrollWidth ≤ clientWidth + 1`」。⚠️ **用户气泡是右对齐**，文本从左边溢出、`scrollWidth` 恒等于 `clientWidth`（必然为真的假绿灯）——必须按**文本自身矩形**朝两边各量一次。用 `[overflow-wrap:anywhere]`（而非 `break-words`，后者不改固有尺寸） | 现行（`MessageList.tsx` / `SubagentPreview.tsx` / `styles.css`；`dock` +4，逐条见过红） |
| v1.70 | **Live Bar 改回居中**（v1.64 曾改成 `justify-start`，无规则依据且与高保真稿相反）。补成**规则 ⑥-F**；断言带**对照**（同一次测量里临时按 `justify-start` 排一遍，证明它能不相等） | 现行（`Conversation/index.tsx`；`dock` +1） |
| v1.71 | **去掉所有卡片的侧边条与左栏选中条**（19 处 / 7 文件）：语义一律由**背景色**承担。⚠️ 三处性质不同的竖线不动（Markdown 引用 / `.thought` 左描边 / 代码行号栏）；子代理步块的角色改由卡内小标题区分 | 现行（`App.tsx` / `Conversation/index.tsx` / `MessageList.tsx` / `SubagentPreview.tsx` 等；无断言依赖边框） |
| v1.72 | **字号令牌化 + 基准上调一档**（2K 屏反馈字体偏小；结构根因：颜色 / 圆角 / 阴影都在 `@theme`，唯独字号散写任意值——315 处 `text-[Npx]` 无单一旋钮）：全量收敛到语义档 `text-2xs(11) / xs(12) / sm(14) / base(16) / 2xl(24)`（2xs 是唯一自定义档，其余用 Tailwind 默认档值与默认行高）；`styles.css` 硬编码同步上调（body / `.md` / `.thought` 13→14，代码视图 / `.md pre` / 表格 11.5→12，语言标签 10.5→11；行内代码 0.88em 随正文自动跟上）。⚠️ 起手态输入框 73px 原本**恰好**装 3 行旧字号（2.89 行），新字号下只剩 2.68 行——按「3 行」的设计意图上调到 `min-h-[81px]`（这是本次唯一非机械替换的连带修正） | 现行（`styles.css` 的 `--text-2xs` + 27 个文件；单测 1162 全绿；冒烟 `dock` 255 / `model` 33 / `fixture` 35） |
| v1.73 | **「本次改动」清单层新增「已提交」标注**（commit 后**不归 0**——归 0 会丢审计轨迹、净值基线是会话首触快照而非 HEAD，改给「翻篇了」的文件一个可见状态）：口径是**文件粒度**，已跟踪且当前内容与 HEAD 一致（**比内容不比 hash**，amend 后一致也算）才标；修改过 / 暂存过 / 未跟踪 / 被忽略 / 盘上不存在一律不标；非仓库或 git 不可用降级全不标。新 IPC `git.committed` 批量判定（只读 `status --porcelain -z --no-renames --ignored --no-optional-locks`；先 `rev-parse --show-toplevel` 换算仓库根坐标系，项目根是仓库子目录也对；越界与不存在的路径在任何 git 访问之前过滤，防「不存在」冒充「已提交」）。渲染层**只在清单层**、以路径集合拼接串为键按需拉取；总账行不标（避免状态爆炸） | 现行（`git.ts` 的 `readCommittedFiles` + `ChangeDrilldown.tsx` 标注 + `git.committed` 通道；单测 1170 全绿；冒烟 `dock` 261/261，期望值由独立 oracle 现算不写死） |
| v1.74 | **压缩上下文进行中的状态与进度提示**（用户反馈：压缩时会话区毫无提示）：根因是 `running` 布尔把内核 `operation.kind`（run / compaction / navigation）压扁——渲染层无从知道「在压缩」，只有通用「运行中」+ 一段无署名的摘要流。`ConversationView` 新增 `runningOperation`（内核 kind 直投），两处呈现：① Live Bar 主文案「正在压缩上下文」（计时 / 活动照旧）；② 消息流尾部状态头**常驻**（`data-conv-compacting`，脉冲点 +「生成摘要中，完成后历史将替换为摘要」——摘要请求发出到首个 token 之间 `streamingText` 还是 null，状态头不常驻就还有一段「无提示」窗口），流式摘要文本即进度；该行署名从「Agent」改 `null`（那段文本是摘要不是回复） | 现行（`project()` 的 `runningOperation` + `Conversation/index.tsx` 两处分支 + 四处受控视图夹具补字段；单测 1172 全绿；冒烟 `dock` 264/264） |
| v1.75 | **压缩完成后的「压缩卡」**（用户反馈：compact 后历史整片清空、摘要无处可看，像开了新会话）：根因在投影——内核压缩完成把 transcript 整体替换成「compaction 条目 + 尾部保留」，而 `projectTranscript` 只认 `type === "message"`，compaction 条目被静默跳过。`ViewMessage` 新增 `compaction?: { tokensBefore }` 标记（仿 `skill?` 先例：有值即特殊条目），投影时 compaction 条目 → `role: "other"` + 摘要正文入 `text`；渲染层 `groupTurns` 让带标记的条目**自成一张卡并开新轮**（尾部保留消息归进这一轮、`turnOfMessage` 同步对齐——不进轮结构就永远渲染不出来，窗口是按轮切的），卡片画在压缩发生的时序位（transcript 头部）：标题「已压缩上下文」+「压缩前约 N tokens」（数字口径与完成通知 `compactDoneMessage` 一致，`<=0` 不显示数字）+ 摘要正文（Markdown）。目录 / 搜索 / 点链照旧只认 user/assistant，压缩条目不参与提问轮次 | 现行（`project.ts` 的 compaction 分支 + `turn-groups.ts` 的 `compaction` 轮 + `MessageList.tsx` 的 `CompactionCard`（`data-conv-compaction`）；单测 1178 全绿；冒烟 `dock` 270/270） |
| v1.76 | **分叉行左侧的「问答结束时间」**（每轮问答的可读节拍点，挂在已有的④分叉行里、不新增行）：时间取该轮**最终回复的 `timestamp`**（= 问答结束时刻），当天只显示 `HH:mm`、跨天补 `MM-dd` 前缀（长会话跨天常见，只给时分会让两天的消息看起来发生在同一个点）；旧会话消息没有 `timestamp` 就**不显示**（不猜「现在」，与「净值算不出就不显示数字」同一条纪律）。仅非尖端轮有分叉行，故尖端（正在跑的最后一轮）没有时间——等它结束、成为历史轮就有了 | 现行（`MessageList.tsx` 的 `formatTurnClock` + `branchRow` 内 `data-conv-turn-clock`；单测 1178 全绿；冒烟 `dock` 272/272——新增跨天 / 当天两态断言 + tallTurn 无时间戳场景顺带覆盖容错；原「整行不署名 Agent」断言同步更新。⚠️ 该模式总断言数随主仓库工作区现状浮动 ±1：oracle 候选文件有未提交改动时才多一条「修改中的文件不标」，上轮 270 / 本轮 272 之差即来源于此，非缺陷） |
| v1.77 | **⑦ 新增「文件」页签（整项目只读浏览器）**：与「任务摘要」下钻分工——那边是「本次动过什么」（会话视图数据），这边是「项目里有什么」（`file.list` 实时列目录）。左右分栏：左 220px 懒加载目录树（展开才拉、目录在前数字序、`.git`/`node_modules` 隐藏**且头部如实标注**、单层 500 条截断如实说、单层失败只挂在该节点下不拖垮整树），右侧复用 `FilePreview`（安全边界全在主进程）。只读——**没有**新建/重命名/删除：写盘是 agent 工具与审批的领域，用户侧再加写入口会破坏「所有落盘动作可裁决」。新 IPC `file.list`（边界与 `file.read` 同一套：根由主进程按 sessionId→项目推出，三重校验含 realpath 挡 junction 逃逸） | 现行（`file-list.ts` 的 `listDirWithin` + `FilesPanel.tsx`（`data-files-tree`/`data-file-entry`/`data-dir-entry`）+ `WorkspaceDock` 的 `files` kind；单测 1189 全绿；冒烟 `dock` 新增 6 条：开页签/隐藏标注/懒加载/预览/越界拒（走真 invoke）/关页签） |
| v1.78 | **⑦ 新增「终端」页签（交互式 shell，xterm.js + 主进程 PTY）**：**用户直操作能力面**——模型工具面零入口（不走审批：用户直敲、没有可裁决对象，同浏览器 B1 的用户侧按钮；将来接给 agent 必须作为工具过 `before_tool` 闸门，`SECURITY.md` §五）。载体是 `@xterm/xterm`（渲染层）+ `@homebridge/node-pty-prebuilt-multiarch`（主进程 ConPTY，prebuilt 原生模块；Electron 44.2 与 Node 24 ABI 同为 149，本机 SAC 实测不拦 `.node`，`asarUnpack` 已解包）。**生命周期锚主进程**：切页签 / 切会话不杀（渲染层卸载只取消订阅；StrictMode 双挂载靠 `open` 幂等消化），三层收口：页签 × → `terminal.close`、会话 dispose → 自动清、app 退出 → `disposeAll`。shell 由主进程挑（pwsh → powershell → cmd，**渲染层不能指定**）；cwd 钉在会话所属项目根（主进程按 sessionId 推出，同 `file.read` 口径）。输出链 16ms 合帧批量推（`terminal.output` 事件）+ 64KB 环形缓冲（切页签期间的输出靠它回放；`open` 幂等返回 `{replay, nextSeq}`，渲染层**先订阅再 open**、丢 `seq < nextSeq` 的旧帧解竞态）。退出态有覆盖层（「已退出（代码 N）」+ 重新打开），原生模块装不上给明确报错 + 重试，不白屏。**随批订正**：§1「代码现状」的页签清单自 v1.77 起漏同步（当时只加了版本表条目），本条一并补齐——页签现为七 kind，「+」菜单六项（`browser / files / terminal / usage / rules / events`），A3「不做整项目树」的旧决定同步标注翻案 | 现行（`terminal-host.ts`（会话管理 + 三层收口）+ `terminal-pty.ts`（shell 挑选 / PTY 选项纯函数）+ `terminal-feed.ts`（合帧 / 环形缓冲纯函数）+ `TerminalPanel.tsx`（`data-terminal-panel`/`data-terminal-exited`/`data-terminal-error`）+ `WorkspaceDock` 的 `terminal` kind + `closeDockInstance` 收口；单测新增 19（pty 9 + feed 10），全量 1208 全绿；冒烟 `dock` 新增 6 条：开页签 PTY 活 / 键盘输入真回显（KeyboardEvent 带 keyCode 遗留成员）/ cwd 在项目根（提示符即证据）/ 切走切回不死且回放 / 关页签 PTY 真死（主进程 isAlive）/ 重开新 PTY 真活，285/285） |
| v1.79 | **两页签的 review 修复**（v1.77 / v1.78 落地后的代码审查，改动全在未推送提交内）：①「文件」——空目录展开显示「（空目录）」（原先留白，分不清「空的」还是「没加载出来」）；`FsEntry` 删掉 `size`（渲染层**无人消费**，却让每次展开付每文件一次主进程同步 stat，大目录可感知卡顿——白付的成本）；指向祖先的软链接（如 `src/link -> ..`）的展示怪癖在 file-list 头注注明（子条目 path 与真实路径同键、展开态共享，无越界无崩溃，VS Code 同类行为）。②「终端」——**系统收口可见化**：`close(notify)`——会话 dispose / 空闲回收（30 分钟）杀终端时推 `terminal.exit`，面板显示「已退出（已终止）」+「重新打开」（原先这条链**不发事件**，终端假活：敲键被静默丢弃、界面毫无提示——review 抓出的最高优先级缺陷）；退出文案按 exitCode 区分（代码 N / 已终止；原先写进终端的红字被不透明覆盖层盖住，等于没写）；shell 探测结果缓存（原先每次开页签都 `spawnSync where` 阻塞主进程百毫秒级）；pty 的 env 组装不再用 as 断言掩盖 undefined | 现行（`terminal-host.ts` 的 `close(notify)` + `#pickShell` 缓存 / `terminal-pty.ts` 的 `buildEnv` / `TerminalPanel.tsx` 退出态带码 / `file-list.ts` + `protocol.ts` 去 size / `FilesPanel.tsx` 空目录提示；单测 1207 全绿（size 用例随字段删除）；冒烟 `dock` 新增 2 条：系统收口（notify）→ `data-terminal-exited` 可见退出不假活、覆盖层「重新打开」→ PTY 复活） |
| v1.80 | **「文件」页签改版：内容在左、目录树在右，树可整体收起**（用户要求）：原先树在左、预览在右，且树没有整体收起的开关——根层的子条目直接铺在 depth 0，**没有任何一层可收起整棵树**。三条改动：① **分栏对调**——预览（`FilePreview`）到左、懒加载树到右（边框 `border-r` → `border-l`），理由是浏览时**视线与阅读起点都在内容上**，树只是索引，贴右不挡正文；② **树整体收起**——头部新增开关（`PanelRightClose`/`PanelRightOpen`，`aria-expanded` 表达开合），收起时树**整块不渲染**（`data-files-tree-pane` 消失）预览占满全宽，`states`/`expanded` 保留故再展开即时回来、展开态不丢；③ 收起时头部「已隐藏 .git · node_modules」说明同步隐去（树都不在，标注无从对照），空态提示文案改「点**右侧**的文件名」。**注意与节点级展开/收起无关**：逐个目录的 chevron 展开是树自己的既有行为，本次加的是**面板级**开关 | 现行（`FilesPanel.tsx` 的 `treeCollapsed` + 两栏各自锚点 `data-files-preview-pane` / `data-files-tree-pane`（外层 `data-files-tree` 是整个面板，量不出栏的位置，故另给标记——`AGENTS.md` §五⑫）；单测 1207 全绿；冒烟 `dock` 新增 3 条：分栏顺序（两栏量矩形，预览在左且更宽）/ 收起后树整块消失且预览宽度 +100 以上（按收起前实测量算，不写死像素）/ 再展开树回来且 `src/main` 展开态仍在，289/289。**随批订正**：`NEXT-PHASE.md` §1 的 ⑦ 行自 v1.77 起停在「五种 kind / +菜单 4 项」，一并补成七 kind / 六项，并点明 v1.31 取消的旧「文件」页签与 v1.77 的整项目浏览器**同名不同物**） |
| v1.81 | **修左栏「展开项目看不到会话」**（用户反馈：便携版启动后除第一个目录外都空着）：根因是会话列表**只由「把它选成当前项目」触发拉取**（切项目那个 effect），而侧栏的展开按钮只切 `expandedProjects`、不拉数据——于是展开一个没选中的项目会照**空缓存**画成「还没有会话」，其实库里全是会话。用户看到「新建会话才展示历史」正是同一个原因的另一面：点该项目行的「+」会把它置为当前项目，那一下才把历史拉出来。修法：**展开即拉取**（只在「本次是展开」时拉，收起不动缓存，下次展开直接画）；渲染层把 `undefined`（还没拉到 →「加载中…」）与空数组（确实没有 →「还没有会话」）分开。⚠️ **与便携版无关**：全仓 `portable` 只出现在 `electron-builder.yml` 的产物 target 里，数据目录运行期只按 `isDev` 分流（`%APPDATA%\Colt` vs `Colt-dev`），渲染层没有任何 `isDev` / `isPackaged` 分支——只是「只有一个项目」时才撞不上 | 现行（`App.tsx` 的 `onToggle` + 侧栏会话区；`npm run typecheck` / `npm test` 1207 全绿 / `npm run build`） |
| v1.82 | **修输入工具行在中栏最窄时换行 + `/compact` 收成图标**（用户反馈：会话区缩到最窄，输入框底部内容换行了）：根因是 `/compact` 的文字是裸文本、不在 `.lbl` 里，不吃 ≤560 那档标签折叠，于是它成了整行最后一个保持全宽的宽件；中栏一窄（约 ≤416 起无样式兜底、≤400 走「右组 `width:100%` 整块换到第二行」的旧降级）模型/发送就被挤下去。修法（按用户后续要求收敛）：① **`/compact` 按钮直接改为图标-only**（文字全宽度移除，悬停 tooltip「压缩上下文（也可在输入框敲 /compact 回车）」承载可发现性——命令写法仍在 tooltip 里教）；② **撤掉 ≤400 那套换行降级**（`cpush` 隐藏 + `cright` 占满第二行）——图标化后中栏压到下限 360（卡片内容约 324px）单行放得下，`flex-wrap` 仍留着当「标签全折叠后仍装不下」的最后兜底 | 现行（`Conversation/index.tsx` 的 `/compact` 按钮 + `styles.css` ≤400 档；typecheck / 1207 单测全绿。冒烟 `dock` 对该按钮只断言存在性（`data-slash-command`），不受影响。⚠️ 几何量按 13px 图标 / 12px 字号**推算**，未跑实机窄栏测量——沿用「中栏压窄看单行」的人工验证路径 |
| v1.83 | **输入框两态高度微调**（用户要求：「起手态降一点，非起手态高一点」）：起手态 `min-h` 81px（3 行）→ **69px（2.5 行）**；非起手态原先没有 min-h、空内容只有 1 行约 31px → 定 **54px（2 行）基线**。两处都只是下限，多行输入仍由 `resizeInput` 长到 180px 上限，`min-h` 与内联 height 的相互作用是安全的：`height:auto` 时 `scrollHeight ≥ clientHeight ≥ min-h`，回写不会低于下限也不会振荡。**随批改 `model` 冒烟断言**：「起手态输入框占 3 行高」→「占 2.5 行高」（行数由探针按元素自己的行高/内边距现算，tolerance 0.15 不变；行高 = 1.625×字号那条防 tailwind-merge 静默删 leading 的断言原样保留） | 现行（`Conversation/index.tsx` 的 textarea className + `model.ts` 断言；typecheck / 单测全绿。⚠️ `model` 冒烟未实跑（要拉起整应用），2.5 行的算法核过：(69−12)/22.75 = 2.51，在 tolerance 内 |
| v1.84 | **左栏新增全局「新建会话」按钮**（用户要求：在项目上方区域加一个新建会话按钮）：原先唯一入口是**每个项目行自己的「+」**，而它是 `opacity-0 group-hover:opacity-100`（悬停才显形）——多项目时想随手开一条新会话得先扫一遍找到那一行。修法：在 `<aside>` 最顶部、`SidebarSection title="项目"` 之上加一行**常驻的整行按钮**（`Plus` + 「新建会话」，`data-sidebar-new-session`，描边样式同头部「改动 / 设置」）。**目标项目按既有规则取 `activeProject`**（`App.tsx` 的 `conversationProject` 注释已把「左栏高亮 / 展开谁 / 新建会话建到哪」归给它），不新造第二套语义；点击与项目行「+」一致——先 `setMainView("chat")` 再 `newSession(activeProject.id)`，仍是只分配 id 的草稿（不进侧栏，见 ③-C）。**一个项目都没有时置灰**（`disabled` + tooltip「先打开一个项目」），不做成点了没反应的死控件（`AGENTS.md` §三.6）。与项目行「+」是**同一功能的两个入口**，不是替代（规则 ③-E） | 现行（`App.tsx` 侧栏顶部按钮；typecheck / 单测 1207 全绿；冒烟 `model` 新增 5 条——按钮在可视区且命中测试 / 点击得到新草稿 / 侧栏不多出行 / 那条草稿未落库 / 建到当前选中的项目（判据「起手区目录没变」，不硬编码项目名），**38/38** 实跑；项目行「+」的探针按 `[data-project-row]` 作用域取 `button[title="新建会话"]`，新增的全局按钮不干扰它；实跑截图确认落位） |

> **改本文档需记录**：改了什么区域、依据什么事实、影响哪些已有设计。
