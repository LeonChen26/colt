# AGENTS.md · 在这个仓库里工作必须先读

> 这份文件记录**已经犯过的错误**，目的是不再犯第二次。
> 每一条都对应一次真实的翻车事故，不是假想的风险。
> 新增条目时：写清「症状 → 根因 → 铁律」，不要只写结论。
>
> **配套文档**（本文件只管「已犯过的错」，别的内容各有宿主）：
> 项目是什么 / 怎么跑 → `README.md`；设计原则 → `docs/PRINCIPLES.md`；
> 跨进程边界与契约变更 → `docs/ARCHITECTURE.md`；权限模型与安全边界 → `docs/SECURITY.md`；
> 失败如何呈现 → `docs/ERRORS.md`；编号看不懂（`⑦`/`A3-4`/`N1`/`事 B`）→ `docs/GLOSSARY.md`。

---

## 一、文件修改纪律（血泪教训最多的一类）

### 1.1 禁止用脚本反复改写同一个文件

**事故**：在设计稿迭代中，为了「验证拖拽方向」，连续多轮用 `python` 脚本往
`docs/prototype-work-browser-hifi.html` 注入测试代码、修改、再注入。
最终把页面搞到「拖拽失效」，还一度误判为「拖拽逻辑丢了」。

**根因**：
- 每次注入都基于**当时**的文件内容做字符串替换，前一次的残留会污染后一次
- 注入的测试 IIFE 插在业务逻辑之前，一个异常就中断后续所有脚本
- 把「验证」和「修改」混在同一条路径上，出问题时无法判断是改坏了还是本来就没跑

**铁律**：
1. **改文件用 `edit`，一次改到位**。不要用 `bash` + 脚本做多轮注入式修改。
2. **验证与修改分开**：先改完、再验证。不要边改边验、反复横跳。
3. 如果测试手段连续 2 次失败，**停下来问用户**，不要加大注入力度硬试。

### 1.2 验证手段不奏效时，不要改被测对象

**事故**：模拟 pointer 事件验证拖拽，多次失败后，我不断往页面里加钩子
（改 `document.title`、加 `window.__coltDragTest`、加临时自检函数），
而不是去检查「测试是否正确接入」。

**铁律**：
- 测试失败 → 先怀疑**测试接入方式**（时机、选择器、事件目标），
  再怀疑被测代码，**最后**才考虑改被测对象。
- 「模拟 pointer 事件验证拖拽」本身需要事件监听器**已经注册**才能生效；
  脚本在 DOMContentLoaded 前执行就会静默失败——这类时机问题优先排查。

### 1.3 交付前必须清理调试残留

**事故**：多次在文件里留下 `document.title='D:'+...`、临时自检 IIFE、
`console.log('OVERFLOW_TEST ...')`，直到用户看代码才发现。

**铁律**：
- 调试代码**当场清**，不要攒到最后。
- 清理后**读一遍文件结尾**确认（`read` 最后 20 行），不要凭记忆判断清干净了。

---

## 二、需求理解纪律

### 2.1 「删掉 X」时，必须先确认 X 是否有多处、是否有替代入口

**事故（两次，同一类错误）**：

- **事故 A**：用户说「模型选择跟输入框冲突了」→ 我删掉了**输入框里**的模型选择器。
  用户真实意图是：删掉 **Live Bar 里**那个重复的，保留输入框里这个（它才是本体）。
- **事故 B**：用户说「Live Bar 的中断按钮也不要」→ 我删掉后**又自作主张在会话头部
  补了一个 `title="中断当前运行"`**。用户从没要求加，结果屏幕上出现两个停止入口。

**根因**：把「删掉某处的 X」理解成「X 这个功能不要了」，
于是要么删错位置，要么「贴心地」在别处补一个。

**铁律**：
1. 用户说删某个元素时，**先确认**：这个元素在界面上有几处？删的是哪一处？
2. **不要自己补替代入口**。要补，先问。
3. 「冲突」「重复」「多余」这类词，指向的是**重复关系**，不是**功能本身**。

### 2.2 改动前先对照设计文档的清单

**事故**：出高保真稿时漏掉了 Live Bar——而它在 `docs/UI-DESIGN-v3.md` 里
被标为 **P0、「v3 最重要的新组件」**，我前面还引用过这句话。

**铁律**：
- 做 UI 稿前，先读 `docs/UI-DESIGN-v3.md` 的**§9 能力对照表**和**§10 优先级表**，
  把它们当验收 checklist 逐项核对。
- 交付时明确列出「已完成 / 未完成」的对照，不要等用户发现漏项。

---

## 三、产品判断纪律

### 3.1 不要把现状当约束

**事故**：讨论「浏览器该内嵌还是弹窗」时，我看到 `browser-host.ts` 的注释
写着「便于用户旁观与授权」，就接受了「独立窗口」这个前提，
直到用户质疑「为什么不融合到界面里」才回头分析。

**根因**：被实现细节和既有注释锚定，把「现在这样做」当成了「必须这样做」。

**铁律**：
- 评估架构/交互时，先问「这个前提成立吗」，再问「怎么优化」。
- 代码注释是**当时**的理由，不是不可推翻的结论。
- 主动区分「技术上做不到」和「改起来麻烦」。

### 3.2 说错要立刻认，不要模糊化

**事故**：关于独立窗口的理由，我说过一条「内嵌后用户看不到 agent 在操作」——
这是**错的**，内嵌恰恰更利于旁观。后来才纠正。

**铁律**：论述被推翻时，明确指出**哪一条错了**，不要用「需要权衡」之类的话轻轻带过。

---

## 三、产品判断纪律

### 3.3 坐标 / 位移计算必须代入具体数值验证

**事故**：设计稿右侧拖拽把手失效。根因是「取负取错了对象」：

```js
// 错误：bindGrip 传进来的是 startVal + dx（startVal = 当前宽度）
const px = -raw;          // -（300 + 20）= -320 → 被钳到下限
```

正确写法是**只对位移取负**：

```js
const dx = raw - startVal;
const px = startVal - dx;
```

**根因**：我之前确实“验证过方向”，但验证的是「符号要不要反过来」，
而**没有验证「取负作用在哪个量上」**。判断“要不要取负”远不够。

**铁律**：
- 涉及坐标 / 位移 / 比例的计算，**必须用具体数值代入跑一遍**，
  列出「向左拖 N px → 结果应为 X」的对照表，看数字对不对。
- 不要只做定性判断（“应该反过来吧”）。
- 特别是当公式里有 `startVal + delta` 这种复合项时，
  **先拆成两个独立的量再运算**。

### 3.4 不要假定不存在的能力

**事故**：我让用户“在 Console 里跑一句命令验证拖拽”，
但本项目内置浏览器工具（`browser_act`）**没有执行 JS 的能力**，
用户根本没地方输入。我基于一个不存在的能力设计了验证方案。

**铁律**：设计验证方案前，先确认手里有哪些工具、它们真的能做什么。
引用工具前先看它的能力清单，不要凭印象。

### 3.5 设计前先读代码确认现状，不凭截图推测

**事故（反复发生 5 次，本项目最严重的错误模式）**：

一直以一张**旧截图**作为设计输入，而产品已向前迭代，导致：

| # | 错误 | 真实情况 |
|---|---|---|
| 1 | 漏做 Live Bar | 产品有，且是 P0 |
| 2 | 漏做工具卡 | 代码里每轮渲染多次 |
| 3 | 以为右栏是“跟随线” | 实际是页签工作区 |
| 4 | 以为要新做“文件视图” | 产品已有「文件」页签（⑦-G 后成了「正在处理」下钻的内容层） |
| 5 | 讨论“左栏 L1/L2 分层” | 那两个 UI **根本不存在**（计划中、短期不做） |

**根因**：把截图当成**功能依据**，而它只能当**视觉参考**。
截图是某一时刻的快照，代码才是当前真源。

**铁律**：
1. **动手前先 `read` 相关代码**，确认这个功能/区域**真实存在**、长什么样。
2. 用户给的截图/参考图，**只用来对齐视觉风格**，不用来认定功能范围。
3. 讨论“某个 UI 该怎么改”之前，**先确认它存在**。
4. 设计稿与产品脱节时，**先对齐再设计**，不要继续在分歧上叠加。
5. 在长期维护的设计稿顶部加**状态说明**，标明“本稿 vs 产品现状”的差异。

**判断信号**：当准备说“根据截图…”时，先问自己：这个区域我读过代码吗？

### 3.6 入口只列「真的存在」的能力，否则就是死控件

**事故（两次，同一类）**：

- **⑦-F 那次**：折叠态图标条上的视图图标，点了只切页签却不展开——44px 里看不出任何变化，
  等于**点了没反应**。是冒烟框架跑出来的（肉眼截图看不出来）。
- **A3-3 那次**：高保真稿的「+」菜单画了**终端 / 任务摘要 / 代码变更**，但这三种视图在 ⑦ 里
  **根本不存在**（当时「代码变更」是独立侧栏面板，不是页签；后来 ⑦-G 又把它并进了
  「正在处理」的下钻）。照抄就是三个**死菜单项**。

**根因**：把**设计稿**当成**能力清单**。设计稿是「以后可能长这样」的愿景，**代码才是「现在有什么」**。

**铁律**：
1. 做入口（菜单项 / 图标 / 按钮）前先问：**这东西现在真的存在吗？点下去有可见反馈吗？**
2. 不存在的能力**宁可不放**——不要放 disabled 或点了没反应的占位，它比缺失更伤信任。
3. 加完入口用**冒烟**确认「点了确实有变化」，别只靠肉眼（44px 图标条这类小目标尤其看不出来）。

---

## 四、待观察的习惯（尚未造成事故，但值得警惕）

- **估算代替实测**：修输入区溢出时，我先用 Python 估算宽度再改 CSS。
  估算有用，但**最终必须实测**（中栏压到 420px 看是否真单行）。
- **设计输入契约时凭想象定格式**：A3-2 我把「读文件」的入参定成「只收相对路径」，
  却没先确认**生产方实际会产出什么**——生产方是模型，实测常给绝对路径；而 `read` 又不产生
  改动记录、没有相对路径可回落，这个契约会让「点路径」时灵时不灵（交付前才改掉）。
  铁律：**入参格式取决于生产方，不取决于我们觉得哪种更整洁**；
  安全要靠**校验**（越界判断先于任何 fs 访问）保证，而不是靠限制入参的**写法**。
- **一次改太多**：`edit` 里塞七八个不相关的替换，一旦某个不匹配整批失败。
  分次提交，便于定位。
- **按字段名猜语义（差点做成永久灰条）**：做 ⑥ 的「已中断 / 已失败」时，计划里指向
  `ConversationView.faulted`，名字看着就是「任务失败了」。查内核 `reducer.js` / `lane.js` 才发现：
  它只在 harness 的 `fault` 事件上置位、且**从不复位**，是**会话级硬故障**，不是轮次终态。
  照计划做会得到一个**永久灰条**（比没有信号更糟：它会持续撒谎）。
  铁律：**用外部依赖（内核、宿主、协议）的字段前，先读它的定义与写入点**——
  尤其是那种「名字很自信、没人用过」的字段（`faulted` 在渲染层一处都没被读过）。
  正确信号是 `lastResult.status`（`completed | declined | aborted | failed` + `error`）。
- **把「库提供了函数」当成「库会调用它」（这条**已经进过仓库**，比上一条更贵）**：接 Agent Skills 时，
  我把 skills 放进 `AgentHarnessOptions.resources.skills` 就以为接完了，通知还照常报「已加载 N 个技能」。
  实际上内核**只导出 `formatSkillsForSystemPrompt`、自己一处都不调用**（`resources.skills` 在
  `lane.js` 里只被「按名显式调用」用到）——让模型**看见**技能清单，必须由应用自己拼进系统提示词。
  结果模型根本不知道技能存在，而**装载、告警、计数、typecheck、单测全绿**：彻头彻尾的静默失败，
  是回头 `grep formatSkillsForSystemPrompt` 发现「只有定义、没有调用点」才揪出来的。
  根因：**把「API 表面存在」当成「行为已经发生」**。而 `resources.skills` 确实有效果（显式调用能用），
  「接了一半也能跑」正好把另一半的缺失盖住了——这比「完全没接」更难发现。
  铁律：① 判断某能力「接没接」，不能看有没有可用的函数/字段，要 `grep` 它在依赖内部的**调用点**；
  **只有定义、没有调用 = 必须由应用自己接**。② 「接完了」的判据要落在**行为**上（这里是「模型能不能
  在提示词里看到清单」），不是「参数传进去了」。③ 凡是「参数传了、行为却由库决定」的接线，都该单独
  写一条用例把**最终产物**（这里是组装后的提示词字符串）本身断言下来，而不是只断言输入被接收。
- **回复过长**：用表格和分级标题堆砌，用户可能只想要一句话结论。
  结论先行，细节在后。

---

## 五、环境与工具的事实（避免重复踩坑）

- `read` / `write` / `edit` 可用；**`bash` 在本会话中长期被拒绝**，
  包括 `echo` 这类无害命令。需要执行命令时先确认可用性。
- **删除类操作只允许仓库内路径**（2026-09 实测）：`Remove-Item` / 删除工具都受一份**白名单**约束，
  报 `path not in allowlist`——`%APPDATA%` 下的东西（Colt 的用户数据、临时备份目录）**我删不掉**，
  这类清理只能交给用户（App 里删会话、资源管理器里删目录）。别把「删不掉」当成「删掉了」。
- **读 Colt 的库用 `node:sqlite`**：`new (require("node:sqlite").DatabaseSync)(path, { readOnly: true })`。
  库里用的是 Node **内置** `node:sqlite`，**不是 better-sqlite3**——`node -e` 里 require 后者会
  `MODULE_NOT_FOUND`（它不是直接依赖）。dev 的库在 `%APPDATA%\Colt-dev\data\colt.db`（**不是 `Colt`**）。
- **PowerShell 两个坑（都造成过假结论）**：
  ① 以字符串字面量**开头**的一行会被当成**命令名**——`"a" + (Test-Path x)` 直接报「无法识别」，
  要写 `Write-Output ("a" + ...)`；
  ② `Select-String` 在 PS 5.1 下**默认按 ANSI 读文件**，查 UTF-8 里的中文会**静默零命中**——
  这是个**必然为假的假阴性**（我据此差点报「没找到污染」，加 `-Encoding UTF8` 后立刻查到 16 个文件）。
  判「有没有」之前先确认「查得到」。
- 浏览器工具只接受 **http/https**，不能打开 `file://`。
  预览本地 HTML 需要先起静态服务（`python -m http.server`）。
- `browser_act` **没有拖拽能力**（只有 click/type/scroll/viewport/upload/wait）。
  验证拖拽需要用户手动操作，或用 `computer_action`（坐标易错，慎用）。

  **补充（2026-09）**：拖拽**可以**自动化——走**冒烟框架**而不是 `browser_act`。
  `src/main/smoke.ts` 的 `run()` 就是主进程里的 `webContents.executeJavaScript`，
  能在渲染层**真派发 `MouseEvent`**（拖拽、双击），同时读主进程 `View.getVisible()`
  断言「原生视图是否真的收起」这类截图看不见的状态。右栏（⑦）的折叠 / 拖拽上下限 /
  宽度记忆 / ⑦-F 自动展开 / 折叠收起原生视图 /
  ⑦-G 的「正在处理」（走**真实事件通道**推 `session.view`：段一**不再列已完成文件**、
  底部总账「N 处 · M 文件」双口径、无动作时的空闲空态、点总账落到**下钻清单层**且**页签数不变**）/
  下钻（点 ④ 工具卡的路径 → **内容层**；清单按目录**一层**分组、越界条目被排除**并如实计数**、
  清单 → diff → 内容逐层下钻 + 三条回退出口：面包屑 / 层底「返回」/ ESC）/
  **净值**（多次改动后看的是「最终改成了什么」：卡片给净值、改完又退回原样写「已还原」、
  算不出（没有基线）时**不显示数字**并在清单底部如实计数、展开里的「累计」行、
  落到 diff 层的「累计」档且画的是「基线 → 当前」的真实差异——
  判据是**正文里出现基线痕迹**，不是看 class）/
  页签关闭 + 「+」新增视图（关掉「浏览器」后原生视图必须收起、重开必须回来）/
  面板迁入页签（② 的入口点后面板确实渲染在 ⑦ 内；**多开面板后 `aside` 数不变**，即中栏浮层确已消失）/
  观测抽屉（在真实夹具页上点出控制台报错 / 请求失败 / 下载，再断言抽屉三类都显示；
  **收起抽屉后原生视图 bounds 变高**——抽屉占的是页面区域的高度；
  **点一行展开「条目详情」**——断言完整字段画出来且**没被截断**（量 `scrollWidth <= clientWidth + 1`，
  不看 class）、自动滚进了可视区，最后真的点一次「复制」并从主进程 `clipboard.readText()` 读回来核对）/
  浏览器前进 / 后退 / 刷新（先在夹具站真实加载第二页造历史，再点**界面按钮**走
  「渲染层 → IPC → `navigationHistory`」，判据取主进程读到的**真实 URL**；
  按钮可用性跟着历史走——退到最早一页时后退要变灰）/
  **原生视图与「页面区域」逐像素对齐**（含反复收起/展开 5 轮）与**视口联调标记 / 「恢复」**，
  已由 `COLT_SMOKE_MODE=dock` 覆盖
  （207 条断言，见 `docs/NEXT-PHASE.md` §5 第 3 条）；`fixture` 模式另有 25 条（浏览器能力本体）。
  十三个坑：① 拖拽要拆成
  「按下」与「移动+抬起」两次 `executeJavaScript`，否则监听器还没挂上就丢了 move 事件；
  ② 断言要用与产品同源的公式算期望值，别写死像素。
  ③ **别用 `button[aria-pressed]` 当「页签」的选择器**：容器里挂着的其它可切换控件
  （观测抽屉的三个页签）也用它表达选中，会把页签数多算——按 `data-dock-tab` 认。
  这类「用例选择器太松导致假红」要先怀疑用例，别去改产品（见 §1.2）。
  ④ **`goBack()` / `goForward()` 是异步的**：调用后立刻读 `getURL()` 拿到的还是**旧页面**。
  要知道去了哪，得在调用**之前**从 `navigationHistory.getEntryAtIndex(index ± 1)` 取目的页
  （见 `BrowserHost.navigate`）——把导航后的状态当同步可读，是这类计算最典型的想当然。
  ⑤ **「电平」的状态别只用「边沿」的事件去同步**：原生视图的矩形必须**一直**等于页面区域（电平），
  而 `ResizeObserver` / `window resize` 都是边沿触发——**漏一次边沿就永久错位**。
  实测抓到过：抽屉再展开时页面区域矮了 132px，视图却停在收起时的高度，于是压住观测抽屉。
  教训有两条：**（a）** 电平状态要周期性重申兜底；**（b）** 只验「会变」不验「相等」等于没验——
  之前只断言「收起/展开时高度会变」，所以这个错位躲过了所有冒烟。`getBoundingClientRect` 与 `setBounds`
  是一对必须对上的量，断言就要按逐像素相等来写。
  ⑥ **同一个不变量要在不同「栏宽」下各验一次**：原生视图对齐只在 823 这种宽栏下验过，
  用户偏偏是在**窄栏**里看到问题的（窄栏的上报频率、抽屉占比、视图尺寸都是独立变量）。
  窄栏补验后（最窄 219 宽）区域与视图同为 `219×581`，一切正常——**所以那次「超出」根本不是它**。
  更要命的是同一句话「超出 / 显示不全」对应**两种完全不同的来源**，别听描述就去改代码：
  **①原生视图比「页面区域」大**（`viewport` 覆盖 → 被窗口边缘裁掉 / 压住抽屉）
  vs **②页面自己比视口宽**（→ 页面内裁掉）。
  分开量：主进程 `view.getBounds()` 对渲染层 `[data-browser-area]` 的 `getBoundingClientRect()`，
  相等即视图侧无责；再量**页面自己的 `innerWidth`** 跟区域宽度比——但这个量**必须用
  「浏览器视图的 webContents」读**，用应用 UI 的 `run()` 只会读到窗口宽度（本仓真把「页面 1424」
  当成过页面读数，差点把结论带偏）。
  ⚠️ **判 ② 也绝不能用 `documentElement.scrollWidth`**：页面自带 `overflow-x: hidden` 时它会被
  钳到 `clientWidth`，于是永远「相等」——**一个必然为真的假阴性**。曾据此错判「bing 首页不会
  横向溢出」，把用户报的「右边显示不全」整条推给了覆盖。要用 `document.body.scrollWidth`，
  或遍历全元素取 `getBoundingClientRect().right` 的最大值。重测后：bing 首页 `div.hp_body` 有
  **768px 最小内容宽**，停靠区 <783 就真溢出（779 溢 4px → 699 溢 84px），且 `<html>` 是
  `overflow-x: hidden`（**没有横向滚动条，裁掉的部分用户够不到**）。**所以「超出只能是覆盖」是错的**：
  宽右栏下才是覆盖，窄右栏下就是页面自己装不下。还有一条上限要先算：右栏宽度上限 = 窗口内容宽 − 601
  （左栏 241 + 中栏下限 360），**窗口窄于约 1384 时右栏永远够不到 783**——先看这个上限，
  再谈页面装不装得下。
  还有一层：**探针读数反常时，先确认那个量当时本身是多少**——一度读到「视图 823 而 `innerWidth` 543」，
  差 280px 很像 bug，其实是那一轮右栏恰好就是 543 宽（页面对得严丝合缝）。
  同一趟还撞出 §3.6 的一个新变种：**「在 DOM 里」不等于「用户点得到」**。把联调标记做得更显眼（变宽）之后，
  它是 `shrink-0` 的，窄栏下被地址栏顶出窗口右边——`data-browser-viewport-reset` 查得到、
  `el.click()` 也照样"命中"，但它已经不在可视区里了，**靠 DOM 断言永远发现不了**。
  改成 `document.elementFromPoint(按钮中心)` 做命中测试后立刻变红（当时那趟 115 条里，就是这么抓出来的）。
  铁律：小目标入口（徽标、按钮、页签）的断言要看**它是否真的落在可视区且那一层就是它**，别只查存在。
  ⑦ **写剪贴板要求文档处于聚焦状态**（N1 那次）：`navigator.clipboard.writeText` 在文档未聚焦时
  **直接 reject**，而冒烟跑的时候焦点还在终端上——**静默失败**，表现是「点了没反应、剪贴板里还是旧内容」。
  断言剪贴板内容前先 `window.focus()`（真实用户点按钮时窗口必然聚焦，所以这不是产品缺陷，
  是**测试环境**缺了那个前提）。顺带一条：**别断言内核给出的错误字符串**——
  `http://127.0.0.1:9/refused` 实测报的是 `net::ERR_UNSAFE_PORT`（9 在 Chromium 受限端口名单里），
  不是想当然的 `ERR_CONNECTION_REFUSED`；只认 `net::ERR_` 前缀。
  ⑧ **输入框命令的匹配必须「整串相等」，不能前缀匹配**（`/compact` 那次）：
  初版用 `split(/\s+/, 1)` 取第一段比对，于是 `/compact 一下` 也算命令——
  用户那句「用 /compact 手动压缩」就被**静默吞掉**了（单测当场抓出）。
  判错方向的代价**不对称**：漏认只是原样发出去（用户看得见，再敲一次就好），
  误认是**把用户输入弄丢**（不可见）。所以宁可严一点，未知的 `/xxx` 一律放行。
  顺带：这类「命令识别」的判据别用界面文字，**在 `sessionManager` 上打桩计数**才靠得住——
  「真的走了哪条路径」只有主进程侧的调用记录能作证。
  **补注（v1.42 `/skill`，v1.43 修订）**：这条纪律防的是**静默**吞掉输入，不是「未知一律不许有反应」。
  `/skill <名字>` 就**刻意不回落**——名字是用户自己在磁盘上定的，打错时给一条**可见报错并列出可用名**
  最有用（内核的 `UnknownSkill` 只带名字、不带候选，所以得先在**本地**自查一遍名字）。
  ⚠️ **这次自查在 v1.43 从 worker 前移到了渲染层**（新增 `ConversationView.skills` 通道）：
  只有渲染层能在**提交那一刻**判成败，也就只有它能做到「拦下时**不清空输入**」——
  原先「先清空再发、由 worker 报错」会把用户那半句额外指示一起吃掉。worker 那条降级为**兜底**。
  判据是「用户知不知道自己的输入去哪了」，不是「有没有偏离这条规则」。
  ⑨ **验「把页面等比缩小」必须量页面自己的 `innerWidth`**（v1.40 做「适应宽度」时定的）：
  这类功能的失败模式是「界面改了、页面没缩」——百分比标签、横条是否消失都是**渲染层的自述**，
  两者一致地错时会一起骗过断言。页面侧 `innerWidth` 还会随**区域宽变化**而变，
  所以期望值要按「区域宽 ÷ 预期比例」现算，不能写死：最窄栏（219）应得 365，拉到 500 则应得 700。
  判据取「页面读数 ≈ 现算的期望值」才分辨得出「比例跟着新宽度重算了」还是「停在上一次的比例」——
  后者在 500 宽栏下会把页面撑成 833，而**横条一样会消失**（420 ≤ 500），只断言横条根本抓不住。
  ⑩ **测试装置不许有副作用：打桩别「转发给真实现」**（`dock` 冒烟自己发 `/compact` 那次，2026-09）：
  症状：用户报告「启动 dev 时会自动发一条 `/compact`」，怀疑是产品自己发的
  （落盘产物 `out/.smoke-dock.png` 证实那次是 `dock` 冒烟在跑）。
  根因：`dock` 的 `/compact` 段在 `sessionManager` 上打桩计数，但三个桩都**转调了真实现**，
  于是那两句测试文本（`/compact 帮我看看`、`/usr/local/bin/node`）成了**真 prompt**——
  一边真打模型计费，一边写进**用户真实项目里的真实会话历史**（实测那条会话记录带着 provider / modelId / usageId）。
  排查路径值得照抄：① 先问「产品有没有这条路」——grep 下来 `compact()` 只由用户按钮 / 回车触发，
  没有任何自动路径；② 再问「这个字符串是谁写的」——全仓库只有 `smoke.ts` 里那一句在发；
  ③ 最后用**落盘物证**钉死：`out/` 里冒烟产物的时间戳（那份不是我跑的文件名）+ 会话 JSONL 里那条 user 消息。
  **别停在「大概是产品 bug」，也别停在「可能是环境变量」——两边都要证据。**
  铁律：测试装置的每一次转发都要先问「真发出去会有什么后果」；断言若只依赖**调用被记录**，
  就**只记账**，不要转给真实现。副作用清单里最贵的三项是：**计费 / 写用户数据 / 往真实会话里塞假消息**。
  ⑪ **冒烟夹具里的「整份视图」必须钉成契约类型 `ConversationView`**（v1.43 `/skill` 那次，2026-09）：
  症状：`dock` 新增的 4 条 `/skill` 断言**全红**，报的是「本地拦不住」，看上去像产品坏了。
  根因：`smoke.ts` 里 `smokeView()` 造的受控视图会经 `session.view` **整份替换**渲染层手里那份真实视图，
  而它的返回类型当时写的是 `Record<string, unknown>`——**少写字段编译器不管**。v1.43 给契约新增了
  `skills`，这份夹具没跟上：渲染层于是把「本会话技能清单」读成 `undefined`（＝**不知道**，按设计要放行），
  本地拦截整条失效。**「夹具缺字段」和「产品有缺陷」在红字里长得一模一样**；同一趟还漏着
  `thinkingLevel`（更早就有，只是没人依赖它才一直没暴露）。
  铁律：① 夹具若**整份替换**真实数据，它的类型就**必须是那个契约本体**（这里是 `ConversationView`），
  覆盖项写 `Partial<契约>`——这样契约再加字段时夹具会**编译不过**，而不是静默把字段抹成 `undefined`；
  ② 一簇新断言**全红**且都指向同一个「没生效」时，先按 §1.2 怀疑**测试接入**（这里是夹具），别先改产品；
  ③ 还要分清两份「视图」：`sessionManager.getView()` 是**主进程**那份状态，渲染层手里那份是**推过去**的——
  这次前者一直是对的，被盖掉的是后者，只读前者会得出「数据明明是好的」这种**必然误导**的结论。
  ⑫ **探针别靠「层级」猜元素：给被测结构一个它自己的标记**（v1.44 `/` 候选浮层那次，2026-09）：
  症状：给 `textarea` 套了一层定位容器之后，2 条附件断言变红（报「输入卡片内没找到提示」），
  而另 1 条——`model/no-usable` 的「输入卡片完整落在窗口内」——**没红，但它已经在量别的东西了**。
  根因：两处探针都把「`textarea.parentElement`」当成输入卡片（那曾是实测结构）。套上容器之后
  `parentElement` 变成了那层容器：前两处的 `card.querySelector("[data-conv-attach-notice]")` 自然
  找不到提示；后一处则改为去量一层**恒在卡片内部**的元素——它的矩形永远落在卡片之内，那条断言
  **从此必然为真**（真被 `overflow-hidden` 裁掉也照样过）。**一条必然为真的断言比一条红断言贵得多**：
  它不会引起任何人注意。
  铁律：① 探针要认的如果是「某一类界面区域」，就用**那个区域自己的标记**（`[data-conv-card]`、
  `[data-conv-error]`、`data-dock-tab` 都行），别用 `parentElement` / `children[i]` / 靠 class 碰运气；
  ② 改动一个元素的**祖先链**时，顺手 `grep` 一遍全靠结构认它的探针（本例 grep `parentElement`
  就两处，一下就找齐）；③ 一簇断言变红时先分清哪些是**真坏**、哪些是**探针量错了地方**——
  本例那 2 条红是探针错（产品是好的），而那 1 条**没红**的才是真隐患（同 §1.2）。
  ⑬ **用例的「环境前提」必须显式建立，而且只写一次**（修 `model` 冒烟那次，2026-09）：
  症状：`model` 模式从 **34/34 掉到 30/34**，红的是 `[model/fallback]` / `[model/keyless]` 各 2 条
  「默认解析落到…」——看上去像模型解析坏了。
  根因：这三段（含**没红**的 `[model/no-usable]`）假定的都是「这台机器上**只有**用例造的那个服务」，
  而默认解析 `resolveSessionModel` 是从**整份** provider 列表里挑的。其中**只有一段**把这个前提
  显式建立起来了（先把真实服务挪开、`finally` 里还回去），另两段只挪得动内置 `deepseek`——
  于是本机自己配的 `glm`（带密钥）抢走了默认解析。**同一类前提写了两遍、漏了一段，是这类问题的
  标准形态**；而没漏的那些段还都是绿的，更容易让人以为「这个套路大家都懂」。
  铁律：① 凡是假定「机器上只有 X」的用例，都要**显式把别的挪开**，别指望用户机器是干净的；
  ② 同一类前提**抽成一个函数**（这里是 `stashUsableProviders()`），让「漏掉一段」在结构上不可能；
  ③ 挪用户的真实配置要**挑副作用最小的做法**——能删密钥就别删条目（硬崩也只丢一个密钥值），
  能不删就别删，并保证 `finally` 里**先还密钥、再写回条目**；
  ④ 判「是不是产品坏了」要看那条断言**依赖什么**：这次它只依赖
  `resolveSessionModel(…, listProviders())` 与库里那唯一一行 `glm`，所以是环境而不是代码。
- 页面里有 `setInterval` 心跳时，`browser_act wait mode=idle` **永远超时**，
  因为 DOM 每秒都在变。改用 `sleep` + 直接读取。
- **本机开着「智能应用控制」（SAC），会拦未签名的 `electron.exe` → `npm run dev` 起不来。**
  （2026-09 实测）症状：`electron-vite` 报 `Error: spawn UNKNOWN`（errno **-4094**），
  而 `npm run build` / `npm test` 全绿（它们不拉起 Electron）；PowerShell 直跑那个 exe 会说
  「**应用程序控制策略已阻止此文件**」。判据：

  ```powershell
  & .\node_modules\electron\dist\electron.exe --version   # 报「已阻止」= 被策略拦
  (Get-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\CI\Policy").VerifiedAndReputablePolicyState
  # 1 = 强制（0 关 / 2 评估）
  ```

  **根因不是签名，是信誉**：官方 Electron 的 `electron.exe` **本来就不签名**（从官方 zip 里抽出
  与我装的那份逐字节相同，`NotSigned`）；SAC 按微软信誉库放行——`esbuild.exe` 同样未签名却能跑。
  实测（同机同目录同抽取方式，都是官方件）：
  **44.3.0（发布 7 天）✗ / 44.2.0（11 天）✓ / 44.0.0（21 天）✓ / 43.3.0（42 天）✓ / 40.10.2（111 天）✓**
  → 阈值在 **7~11 天**之间。**停在新版本上就会「今天能跑、过几天又跑不了」**。
  铁律：① `electron` **精确 pin**，别用 `^`（`^44.2.0` 会解析回被拦的 44.3.0）；
  ② 升 Electron 之后**必须真起一次** `npx electron --version`，别只看 `build` 过没过；
  ③ 报 `spawn UNKNOWN` 时先按上面两条量，**不要**先去改项目代码（与业务代码无关）。
