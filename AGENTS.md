# AGENTS.md · 在这个仓库里工作必须先读

> 这份文件记录**已经犯过的错误**，目的是不再犯第二次。
> 每一条都对应一次真实的翻车事故，不是假想的风险。
> 新增条目时：写清「症状 → 根因 → 铁律」，不要只写结论。
>
> **配套文档**（本文件只管「已犯过的错」，别的内容各有宿主）：
> 项目是什么 / 怎么跑 → `README.md`；怎么改（环境、命令、测试与冒烟、代码地图）→ `CONTRIBUTING.md`；设计原则 → `docs/PRINCIPLES.md`；
> 跨进程边界与契约变更 → `docs/ARCHITECTURE.md`；权限模型与安全边界 → `docs/SECURITY.md`；
> 失败如何呈现 → `docs/ERRORS.md`；编号看不懂（`⑦`/`A3-4`/`N1`/`事 B`）→ `docs/GLOSSARY.md`。

---

## 一、文件修改纪律（血泪教训最多的一类）

### 1.1 禁止用脚本反复改写同一个文件

**事故**：为「验证拖拽方向」，连续多轮用 `python` 往 `docs/archive/prototype-work-browser-hifi.html` 注入测试代码、修改、再注入，把页面搞到「拖拽失效」，还一度误判为「拖拽逻辑丢了」。
**根因**：① 每次注入基于**当时**文件内容做字符串替换，前次残留污染后次；② 测试 IIFE 插在业务逻辑之前，一个异常就中断后续所有脚本；③ 「验证」与「修改」混在一条路径上，出问题无法判断是改坏了还是本来就没跑。
**铁律**：① **改文件用 `edit`，一次改到位**——不要 `bash` + 脚本多轮注入；② **验证与修改分开**，不要边改边验、反复横跳；③ 测试手段连续 2 次失败就**停下来问用户**，别加大注入力度硬试。

### 1.2 验证手段不奏效时，不要改被测对象

**事故**：模拟 pointer 事件验证拖拽多次失败后，我不断往页面加钩子（改 `document.title`、加 `window.__coltDragTest`、加临时自检函数），而不是去检查「测试是否正确接入」。
**铁律**：① 测试失败先怀疑**测试接入方式**（时机、选择器、事件目标），再怀疑被测代码，**最后**才考虑改被测对象；② 「模拟 pointer 事件验证拖拽」需要监听器**已注册**才生效，脚本在 DOMContentLoaded 前执行会静默失败——这类时机问题优先排查。

### 1.3 交付前必须清理调试残留

**事故**：多次留下 `document.title='D:'+...`、临时自检 IIFE、`console.log('OVERFLOW_TEST ...')`，直到用户看代码才发现。
**铁律**：调试代码**当场清**；清理后**读一遍文件结尾**（`read` 最后 20 行）确认，别凭记忆。

### 1.4 碰「已知大户」前先算净增行数，别等闸红了再拆

**事故**（2026-09，做 `ask_user` 时）：改完 `session-manager.ts` / `worker/entry.ts` 才跑测试，体量闸当场报「多出 **152** 行 / **24** 行」，被迫在同一轮连做三次与本功能无关的抽取（提问队列 → `main/question-store.ts`、纯投影 `project()` → `worker/lib/project.ts`、通知与宿主能力调用 → 两个小模块）。结果对，但**顺序反了**——本该动手前就把「要搬哪块」写进计划。
**根因**：当时 `RATCHET` 上限 = **建闸那天的实测行数**，也就是**零余量**；给大户加功能必须**同时搬走等量旧代码**。这不是意外，是闸的设计。
**现状（2026-09-20 起）**：4 条棘轮先统一为 2200（与通用上限同值），随后**整个去掉**——闸现在只有一条线：`src/` 下（除 `src/dev/`）任何文件不超过 2200 行，另加一条「`Conversation/index.tsx` 的 hook 数 ≤ 50」。**零余量不再成立**，但**「动手前先量」这一步一条都不能省**：大户名单与其原因登记在 `tests/size-guard.test.ts` 的 `KNOWN_LARGE_FILES`，`entry.ts` 这类文件说逼近就逼近，而等闸红了才动手，就是把这一节重走一遍。
**铁律**：① 动手前先量（闸用 `split("\n").length`，与 `wc -l` 差 1，别混用）：`node -e "console.log(require('fs').readFileSync('src/main/session-manager.ts','utf8').split('\n').length)"`；② 量完先决定「搬哪块」再写功能，搬的块要**机械可验证、自成一体**（纯函数 / 一个完整往返 / 一段通知），**不顺手改行为**；③ 搬完立刻 `typecheck && test`（体量闸就在这套里），「位置变了、行为没变」全绿才算搬对；④ 被闸逼出来的结构反而更好读：提问链路最后落在 `question-store.ts` + `QuestionCard.tsx` + `useBlockingCards.ts` 三个新文件，大户只留一行委托——**别把新功能塞进大户**。

### 1.5 写文件前先确认它是不是**已存在**：`Write` 覆盖掉了一个 25 条用例的测试文件

**事故**（2026-09）：给 `project()` 补单测时直接 `Write` 了 `tests/project.test.ts`（7 条），跑全量发现总数从 **805 掉到 787**——那个文件**本来就有**（246 行 / 25 条，覆盖同模块的 `extract*` 与 `projectBranchNodes`），被整个覆盖。`git status` 里它是 ` M`（**已跟踪**），瞄一眼就不会发生。`git checkout` 还原后把新用例**追加**进去，总数回到 812。
**根因**（两条叠加）：① 我拿「`grep "project\("` 在 `tests/` 无匹配」推断「文件不存在」——**推断本身错了**：那个文件恰好不调用 `project(`。**无匹配只说明内容不含该串，不说明文件不存在**；② `Write` 已存在路径前没先 `Read`——把「我以为是新文件」当成了「它是新文件」，**假设没经验证就成了前提**。
**铁律**：① 写新路径前先确认它真的新：`git status --short` 出现 ` M` 就是已跟踪，或先 `Read`（读不到再写）；② **不要用「grep 无匹配」推断文件不存在**；③ 用例总数可预期（改前 N + 新增 M），**对不上当场查**（787 < 805 一看就知道）；查法 `git stash push` 回 HEAD 量基准（本例 805），比对着 diff 猜快得多。

---

## 二、需求理解纪律

### 2.1 「删掉 X」时，必须先确认 X 是否有多处、是否有替代入口

**事故（两次，同一类错误）**：A：用户说「模型选择跟输入框冲突了」→ 我删了**输入框里**的选择器，真实意图是删 **Live Bar 里**那个重复的（输入框里才是本体）。B：用户说「Live Bar 的中断按钮也不要」→ 我删后又**自作主张在会话头补了一个**停止入口，屏幕上出现两个。
**根因**：把「删掉某处的 X」理解成「X 这个功能不要了」，于是要么删错位置，要么「贴心地」在别处补一个。
**铁律**：① 用户说删某元素时**先确认**：它在界面上有几处？删的是哪一处？② **不要自己补替代入口**，要补先问；③ 「冲突」「重复」「多余」指向**重复关系**，不是**功能本身**。

### 2.2 改动前先对照设计文档的清单

**事故**：出高保真稿时漏掉 Live Bar——它在设计稿里被标为 **P0、「v3 最重要的新组件」**，我前面还引用过这句话。
**铁律**：① 做 UI 稿前先读设计文档的**能力对照表**与**优先级表**，并对照 `docs/UI-REGIONS.md`（界面现状真源），当验收 checklist 逐项核对；② 交付时明确列「已完成 / 未完成」，别等用户发现漏项。

---

## 三、产品判断纪律

### 3.1 不要把现状当约束

**事故**：讨论「浏览器该内嵌还是弹窗」时，见 `browser-host.ts` 注释写「便于用户旁观与授权」，就接受了「独立窗口」前提，直到用户质疑「为什么不融合到界面里」才回头分析。
**根因**：被实现细节与既有注释锚定，把「现在这样做」当成了「必须这样做」。
**铁律**：评估架构/交互先问「这个前提成立吗」再问「怎么优化」；代码注释是**当时**的理由，不是不可推翻的结论；主动区分「技术上做不到」与「改起来麻烦」。

### 3.2 说错要立刻认，不要模糊化

**事故**：关于独立窗口的理由，我说过「内嵌后用户看不到 agent 在操作」——**错的**，内嵌恰恰更利于旁观。
**铁律**：论述被推翻时，明确指出**哪一条错了**，别用「需要权衡」轻轻带过。

### 3.3 坐标 / 位移计算必须代入具体数值验证

**事故**：设计稿右侧拖拽把手失效，根因是「取负取错了对象」：
```js
// 错：bindGrip 传进来的是 startVal + dx（startVal = 当前宽度）
const px = -raw;              // -（300 + 20）= -320 → 被钳到下限
// 对：只对位移取负
const dx = raw - startVal;
const px = startVal - dx;
```
**根因**：我确实“验证过方向”，但验的是「符号要不要反过来」，**没验「取负作用在哪个量上」**。
**铁律**：涉及坐标/位移/比例的计算，**必须代入具体数值跑一遍**，列出「向左拖 N px → 结果应为 X」对照表；别只做定性判断；公式有 `startVal + delta` 这类复合项时**先拆成两个独立的量再运算**。

### 3.4 不要假定不存在的能力

**事故**：我让用户“在 Console 里跑一句命令验证拖拽”，但内置 `browser_act` **没有执行 JS 的能力**，用户根本没地方输入——我基于一个不存在的能力设计了验证方案。
**铁律**：设计验证方案前，先确认手里有哪些工具、真能做什么；引用工具前先看它的能力清单，别凭印象。

### 3.5 设计前先读代码确认现状，不凭截图推测

**事故（反复发生 5 次，本项目最严重的错误模式）**：一直以一张**旧截图**当设计输入，而产品已迭代：

| # | 错误 | 真实情况 |
|---|---|---|
| 1 | 漏做 Live Bar | 产品有，且是 P0 |
| 2 | 漏做工具卡 | 代码里每轮渲染多次 |
| 3 | 以为右栏是“跟随线” | 实际是页签工作区 |
| 4 | 以为要新做“文件视图” | 产品已有「文件」页签（⑦-G 后成了「任务摘要」下钻的内容层） |
| 5 | 讨论“左栏 L1/L2 分层” | 那两个 UI **根本不存在**（计划中、短期不做） |

**根因**：把截图当**功能依据**，而它只能当**视觉参考**；截图是某一刻快照，代码才是当前真源。
**铁律**：① 动手前先 `read` 相关代码，确认功能/区域**真实存在**；② 用户给的截图**只对齐视觉风格**，不认定功能范围；③ 讨论“某 UI 怎么改”前**先确认它存在**；④ 设计稿与产品脱节时**先对齐再设计**；⑤ 长期设计稿顶部加**状态说明**标明“本稿 vs 产品现状”。
**判断信号**：准备说“根据截图…”时先问自己：这个区域我读过代码吗？

### 3.6 入口只列「真的存在」的能力，否则就是死控件

**事故（两次，同一类）**：⑦-F 折叠态图标条上的视图图标点了只切页签不展开（44px 里看不出变化，冒烟才跑出来）；A3-3 高保真稿「+」菜单画了**终端/任务摘要/代码变更**，而它们在 ⑦ 里**根本不存在**，照抄就是三个**死菜单项**。
**根因**：把**设计稿**当成**能力清单**——设计稿是「以后可能长这样」的愿景，**代码才是「现在有什么」**。
**铁律**：① 做入口前先问「这东西现在真的存在吗？点下去有可见反馈吗」；② 不存在的能力**宁可不放**（disabled / 点了没反应比缺失更伤信任）；③ 加完入口用**冒烟**确认「点了确实有变化」，别只靠肉眼（44px 图标条这类小目标尤其看不出）。

---

## 四、易犯的思维习惯与陷阱（按「易复发度」归类，含已造成事故的）

- **估算代替实测**：修输入区溢出时先用 Python 估算再改 CSS——估算有用，但**最终必须实测**（中栏压到 420px 看是否真单行）。
- **设计输入契约时凭想象定格式**：A3-2 把「读文件」入参定成「只收相对路径」，却没先确认**生产方实际会产出什么**——生产方是模型，实测常给绝对路径，而 `read` 又不产生改动记录、没有相对路径可回落，会让「点路径」时灵时不灵。铁律：**入参格式取决于生产方**，不取决于我们觉得哪种更整洁；安全靠**校验**（越界判断先于任何 fs 访问），不靠限制入参的**写法**。
- **一次改太多**：`edit` 里塞七八个不相关替换，一个不匹配整批失败——分次提交，便于定位。
- **按字段名猜语义（差点做成永久灰条）**：做 ⑥「已中断/已失败」时计划指向 `ConversationView.faulted`，名字看着就是「任务失败了」。查内核 `reducer.js`/`lane.js` 才发现它只在 harness `fault` 事件置位且**从不复位**，是**会话级硬故障**——照计划做会得到一个**永久灰条**（比没信号更糟：持续撒谎）。铁律：**用外部依赖（内核/宿主/协议）的字段前，先读它的定义与写入点**，尤其是「名字很自信、没人用过」的字段；正确信号是 `lastResult.status`（`completed | declined | aborted | failed` + `error`）。（2026-09-17 补：该字段已连同同样零读取的 `lane`/`cwd` 从 `ConversationView` 删除，铁律仍在。）
- **把「库提供了函数」当成「库会调用它」（**已经进过仓库**，比上一条更贵）**：接 Agent Skills 时把 skills 放进 `AgentHarnessOptions.resources.skills` 就以为接完了，通知还照常报「已加载 N 个技能」。实际内核**只导出 `formatSkillsForSystemPrompt`、自己一处都不调用**——让模型**看见**清单必须由应用自己拼进系统提示词。结果模型根本不知道技能存在，而**装载、告警、计数、typecheck、单测全绿**：彻头彻尾的静默失败，回头 `grep formatSkillsForSystemPrompt` 发现「只有定义、没有调用点」才揪出来。根因：**把「API 表面存在」当成「行为已经发生」**；`resources.skills` 确实有效果（显式调用能用），「接了一半也能跑」正好盖住另一半。铁律：① 判「接没接」要 `grep` 它在依赖内部的**调用点**——**只有定义、没有调用 = 必须由应用自己接**；② 「接完了」的判据落在**行为**上（模型能不能在提示词里看到清单），不是「参数传进去了」；③ 「参数传了、行为由库决定」的接线，都要单独写用例断言**最终产物**本身。
- **把「改了一个约束」当成「这个约束在起作用」（2026-09-20）**：做 v1.60 起手态视觉时，我把输入卡片外层的 `max-w-[796px]` 改成 `880`，并把「宽度上限 796 → 880，卡片实际约 796 → 848」写进了 `UI-REGIONS.md` 的版本表。下一轮做「拉伸右栏」测试，探针打出**中栏 642px 时卡片只有 445px**——两个数都不成立，而且根因跟那个上限毫无关系：外层带 `mx-auto`，**跨轴 `auto` 外边距打断了 flex 的 `stretch`**，卡片一直按内容收缩，`max-w` 在 796 与 880 之间改，全落在「够不着」的那一侧。逐条拆：① 症状是**两处**——产品少了个 `w-full`，文档多了一句没量过的话；② 根因是同一个思维动作：**把「我改了一个约束」当成「这个约束在起作用」**（与上一条同源：把「表面改了」当成「行为变了」）。铁律：**改上限类样式（`max-w` / `min-h` / `max-h`）之前，先量它当前的实际值**——实际值落在上限之内，这次改动就是 **no-op**，文档一个字都不该跟着动；判「生效没生效」的唯一判据是**渲染后的几何量**（`getBoundingClientRect`），不是 diff 里的类名。
- **探针要量的那个量，必须跟着被测对象一起变（2026-09-20，同一个下午的第二次翻车）**：为验「拉伸右栏后起手态不被裁」，我用 `closest('[class*="grid-rows-"]')` 取「会话区」，结果命中**外层两列网格**——它宽度恒等于可用空间（1186），拖右栏它**不动**（变的是那个 1fr 中栏）。于是「右栏变宽 → 中栏变窄」这条**永远为假**、「卡片居不居中」按错的中心比出 **413px 偏心**，两条红全是假的；而同一份读数里真正的问题（卡片 445px）差点被当成「探针噪音」放过。铁律：① 量**容器**还是量**子元素**要先想清楚——外层网格是「中栏 + 右栏」，它天然不随右栏变，要的是 `[网格左, 右栏左]` 这个**算出来**的中栏；② 一条读数**要么随被测变量变、要么按产品的公式算得出来**，两头不沾的（恒定值）比一条红断言更贵——它会让人得出「产品没问题」的结论（同 §五⑫）；③ 一簇红里有真有假时，先按 §1.2 归因，别整批当成噪音划掉。
- **回复过长**：用表格和分级标题堆砌，用户可能只想要一句话结论——结论先行，细节在后。

---

## 五、环境与工具的事实（避免重复踩坑）

- `read` / `write` / `edit` 可用；**`bash` 在本会话中长期被拒绝**（含 `echo` 这类无害命令）。要执行命令先确认可用性。
- **删除类操作只允许仓库内路径**（2026-09 实测）：`Remove-Item` / 删除工具都受一份**白名单**约束，报 `path not in allowlist`——`%APPDATA%` 下的东西（Colt 用户数据、临时备份目录）**删不掉**，只能交给用户（App 里删会话 / 资源管理器删目录）。别把「删不掉」当「删掉了」。
- **读 Colt 的库用 `node:sqlite`**：`new (require("node:sqlite").DatabaseSync)(path, { readOnly: true })`。库里用的是 Node **内置** `node:sqlite`，**不是 better-sqlite3**（`node -e` require 后者会 `MODULE_NOT_FOUND`）。dev 的库在 `%APPDATA%\Colt-dev\data\colt.db`（**不是 `Colt`**）。
- **PowerShell 两个坑（都造成过假结论）**：① 以字符串字面量**开头**的一行会被当成**命令名**——`"a" + (Test-Path x)` 报「无法识别」，要写 `Write-Output ("a" + ...)`；② `Select-String` 在 PS 5.1 下**默认按 ANSI 读文件**，查 UTF-8 里的中文会**静默零命中**（加 `-Encoding UTF8` 才查得到，曾据此差点误报「没找到污染」）。**判「有没有」之前先确认「查得到」。**
- 浏览器工具只接受 **http/https**，不能打开 `file://`；预览本地 HTML 要先起静态服务（`python -m http.server`）。
- `browser_act` **没有拖拽能力**（只有 click/type/scroll/viewport/upload/wait）；有 `setInterval` 心跳时 `wait mode=idle` **永远超时**（DOM 每秒在变），改用 `sleep` + 直接读。验证拖拽要用户手动操作，或用 `computer_action`（坐标易错，慎用）。

  **补充（2026-09）**：拖拽**可以**自动化——走**冒烟框架**而不是 `browser_act`：`src/dev/smoke/` 的 `run()` 就是主进程里的 `webContents.executeJavaScript`，能在渲染层**真派发 `MouseEvent`**（拖拽、双击），同时读主进程 `View.getVisible()` 断言「原生视图是否真的收起」这类截图看不见的状态。`COLT_SMOKE_MODE=dock` 已覆盖右栏（⑦）的：折叠 / 拖拽上下限 / 宽度记忆 / ⑦-F 自动展开 / 折叠收起原生视图 / ⑦-G「任务摘要」（走**真实事件通道**推 `session.view`：段一**不再列已完成文件**、底部总账「N 处 · M 文件」双口径、无动作时的空闲空态、点总账落到**下钻清单层**且**页签数不变**）/ 下钻（点 ④ 工具卡的路径 → **内容层**；清单按目录**一层**分组、越界条目被排除**并如实计数**、清单 → diff → 内容逐层下钻 + 面包屑 / 层底「返回」/ ESC 三条回退）/ **净值**（看「最终改成了什么」：卡片给净值、改完又退回原样写「已还原」、算不出（没有基线）时**不显示数字**并在清单底部如实计数、展开里的「累计」行、落到 diff 层的「累计」档且画「基线 → 当前」的真实差异——判据是**正文里出现基线痕迹**，不是看 class）/ 页签关闭 +「+」新增视图（关掉「浏览器」后原生视图必须收起、重开必须回来）/ 面板迁入页签（**多开面板后 `aside` 数不变**，即中栏浮层确已消失）/ 观测抽屉（在真实夹具页上点出控制台报错 / 请求失败 / 下载，断言三类都显示；**收起抽屉后原生视图 bounds 变高**；**点一行展开「条目详情」**——断言完整字段画出来且**没被截断**（量 `scrollWidth <= clientWidth + 1`）、自动滚进可视区，最后真点一次「复制」并从主进程 `clipboard.readText()` 核对）/ 浏览器前进 / 后退 / 刷新（先在夹具站真实加载第二页造历史，再点**界面按钮**走「渲染层 → IPC → `navigationHistory`」，判据取主进程读到的**真实 URL**；按钮可用性跟着历史走——退到最早一页时后退要变灰）/ **原生视图与「页面区域」逐像素对齐**（含反复收起/展开 5 轮）与**视口联调标记 /「恢复」**（断言条数以运行输出为准，见 `docs/NEXT-PHASE.md` §5 第 3 条）；`fixture` 模式另有浏览器能力本体的用例。
  **`ask-user` 模式**（2026-09，39 条，不跑模型、不计费）：模型提问的阻塞链路——卡片真出现、选项真的能点、多题时**一次只渲染一题**（页码 / 上下一题 / 翻回去选择还在 / 单题不出翻页控件）、每题可**自由输入**且与选项**合并**回传（自填不清空已选）、空问卷不许白屏、输入框回车（非末页翻页 / 末页提交，且**合成中的回车不算**）、提交后主进程收到的载荷键值都对（多选以「、」相连）、跳过走 `skipped` 而不是 `cancelled`、超时自己收尾、`full-access` 下照样弹卡、全程不留悬空卡（含 worker 被回收时）。提问走**真实入队函数** `sessionManager.questions.enqueue()`（就是 worker 发来 `askUserRequest` 时主进程调用的那个），作答载荷用主进程打桩读回（同 `/compact` 那套，但**要转发**——「出队」正是被验的一半，且这次转发不调模型、不写历史）。
  **`ask-user-e2e` 模式**（2026-09，11 条，**打模型、计费**）补的是**入队之前**那段：让模型自己看见并调用 `ask_user`，验 worker 侧 `before_tool` 跳过 `ask_user` 的那条守卫、以及「全权模式下提问仍要弹」（`approvals.listPending` 为空是物证）。做法同 `memory-e2e`：**worker 的生死交给渲染层**（`upsertProject(夹具)` 后 `window.reload()`，等它自动打开会话、就绪）——冒烟直连 `session.open` 会与渲染层的自动打开 + StrictMode 卸载抢同一个 worker，表现为「会话进程在就绪前退出」（见本节末条）。
  十三个坑：① 拖拽要拆成「按下」与「移动+抬起」两次 `executeJavaScript`，否则监听器还没挂上就丢了 move 事件；② 断言要用与产品**同源**的公式算期望值，别写死像素。③ **别用 `button[aria-pressed]` 当「页签」的选择器**：容器里挂着的其它可切换控件（观测抽屉的三个页签）也用它表达选中，会把页签数多算——按 `data-dock-tab` 认；这类「用例选择器太松导致假红」要先怀疑用例，别去改产品（见 §1.2）。④ **`goBack()` / `goForward()` 是异步的**：调用后立刻读 `getURL()` 拿到的还是**旧页面**；要知道去了哪，得在调用**之前**从 `navigationHistory.getEntryAtIndex(index ± 1)` 取目的页（见 `BrowserHost.navigate`）——把导航后的状态当同步可读，是这类计算最典型的想当然。⑤ **「电平」的状态别只用「边沿」的事件去同步**：原生视图的矩形必须**一直**等于页面区域（电平），而 `ResizeObserver` / `window resize` 都是边沿触发——**漏一次边沿就永久错位**（实测：抽屉再展开时页面区域矮了 132px，视图却停在收起时的高度，于是压住观测抽屉）。两条教训：**(a)** 电平状态要周期性重申兜底；**(b)** 只验「会变」不验「相等」等于没验——`getBoundingClientRect` 与 `setBounds` 是一对必须对上的量，断言就要按**逐像素相等**来写。
  ⑥ **同一个不变量要在不同「栏宽」下各验一次**：原生视图对齐只在 823 这种宽栏下验过，用户偏偏是在**窄栏**里看到问题的（窄栏的上报频率、抽屉占比、视图尺寸都是独立变量；窄栏补验后最窄 219 宽区域与视图同为 `219×581`，一切正常）。更要命的是同一句话「超出 / 显示不全」对应**两种完全不同的来源**，别听描述就去改代码：**①原生视图比「页面区域」大**（`viewport` 覆盖 → 被窗口边缘裁掉 / 压住抽屉）vs **②页面自己比视口宽**（→ 页面内裁掉）。分开量：主进程 `view.getBounds()` 对渲染层 `[data-browser-area]` 的 `getBoundingClientRect()`，相等即视图侧无责；再量**页面自己的 `innerWidth`** 跟区域宽度比——这个量**必须用「浏览器视图的 webContents」读**（用应用 UI 的 `run()` 只会读到窗口宽度）。⚠️ **判 ② 绝不能用 `documentElement.scrollWidth`**：页面自带 `overflow-x: hidden` 时它被钳到 `clientWidth`，永远「相等」——**必然为真的假阴性**（曾据此错判「bing 首页不会横向溢出」）；要用 `document.body.scrollWidth` 或遍历全元素取 `getBoundingClientRect().right` 最大值。实测：bing 首页 `div.hp_body` 有 **768px 最小内容宽**，停靠区 <783 就真溢出（779 溢 4px → 699 溢 84px），且 `<html>` 是 `overflow-x: hidden`（**没有横向滚动条，裁掉的部分够不到**）——**所以「超出只能是覆盖」是错的**：宽右栏下才是覆盖，窄右栏下就是页面自己装不下。还有一条上限先算：右栏宽度上限 = 窗口内容宽 − 601（左栏 241 + 中栏下限 360），**窗口窄于约 1384 时右栏永远够不到 783**。另一层：**探针读数反常时，先确认那个量当时本身是多少**（曾读到「视图 823 而 `innerWidth` 543」，差 280px 很像 bug，其实那轮右栏恰好就是 543 宽）。同一趟还撞出 §3.6 变种：**「在 DOM 里」不等于「用户点得到」**——把联调标记做得更显眼（变宽）后它是 `shrink-0` 的，窄栏下被地址栏顶出窗口右边，`data-browser-viewport-reset` 查得到、`el.click()` 也照样"命中"，但已不在可视区，**靠 DOM 断言永远发现不了**；改成 `document.elementFromPoint(按钮中心)` 做命中测试后立刻变红。铁律：小目标入口（徽标、按钮、页签）的断言要看**它是否真的落在可视区且那一层就是它**，别只查存在。
  ⑦ **写剪贴板要求文档处于聚焦状态**（N1 那次）：`navigator.clipboard.writeText` 在文档未聚焦时**直接 reject**，而冒烟跑的时候焦点还在终端上——**静默失败**（表现是「点了没反应、剪贴板里还是旧内容」）。断言剪贴板内容前先 `window.focus()`（真实用户点按钮时窗口必然聚焦，所以这不是产品缺陷，是**测试环境**缺了那个前提）。顺带一条：**别断言内核给出的错误字符串**——`http://127.0.0.1:9/refused` 实测报的是 `net::ERR_UNSAFE_PORT`（9 在 Chromium 受限端口名单里），不是想当然的 `ERR_CONNECTION_REFUSED`；只认 `net::ERR_` 前缀。
  ⑧ **输入框命令的匹配必须「整串相等」，不能前缀匹配**（`/compact` 那次）：初版用 `split(/\s+/, 1)` 取第一段比对，于是 `/compact 一下` 也算命令——用户那句「用 /compact 手动压缩」就被**静默吞掉**了（单测当场抓出）。判错方向的代价**不对称**：漏认只是原样发出去（用户看得见，再敲一次就好），误认是**把用户输入弄丢**（不可见）——所以宁可严一点，未知的 `/xxx` 一律放行。顺带：这类「命令识别」的判据别用界面文字，**在 `sessionManager` 上打桩计数**才靠得住——「真的走了哪条路径」只有主进程侧的调用记录能作证。**补注（v1.42 `/skill`，v1.43 修订）**：这条纪律防的是**静默**吞掉输入，不是「未知一律不许有反应」——`/skill <名字>` 就**刻意不回落**（名字是用户自己在磁盘上定的，打错时给一条**可见报错并列出可用名**最有用；内核的 `UnknownSkill` 只带名字、不带候选，所以得先在**本地**自查一遍名字）。⚠️ **这次自查在 v1.43 从 worker 前移到了渲染层**（新增 `ConversationView.skills` 通道）：只有渲染层能在**提交那一刻**判成败，也就只有它能做到「拦下时**不清空输入**」（原先「先清空再发、由 worker 报错」会把用户那半句额外指示一起吃掉）；worker 那条降级为**兜底**。判据是「用户知不知道自己的输入去哪了」，不是「有没有偏离这条规则」。
  ⑨ **验「把页面等比缩小」必须量页面自己的 `innerWidth`**（v1.40 做「适应宽度」时定的）：这类功能的失败模式是「界面改了、页面没缩」——百分比标签、横条是否消失都是**渲染层的自述**，两者一致地错时会一起骗过断言。页面侧 `innerWidth` 还会随**区域宽变化**而变，所以期望值要按「区域宽 ÷ 预期比例」现算，不能写死：最窄栏（219）应得 365，拉到 500 则应得 700。判据取「页面读数 ≈ 现算的期望值」才分辨得出「比例跟着新宽度重算了」还是「停在上一次的比例」——后者在 500 宽栏下会把页面撑成 833，而**横条一样会消失**（420 ≤ 500），只断言横条根本抓不住。
  ⑩ **测试装置不许有副作用：打桩别「转发给真实现」**（`dock` 冒烟自己发 `/compact` 那次，2026-09）：症状：用户报告「启动 dev 时会自动发一条 `/compact`」，怀疑是产品自己发的（落盘产物 `out/.smoke-dock.png` 证实那次是 `dock` 冒烟在跑）。根因：`dock` 的 `/compact` 段在 `sessionManager` 上打桩计数，但三个桩都**转调了真实现**，于是那两句测试文本（`/compact 帮我看看`、`/usr/local/bin/node`）成了**真 prompt**——一边真打模型计费，一边写进**用户真实项目里的真实会话历史**（实测那条会话记录带着 provider / modelId / usageId）。排查路径值得照抄：① 先问「产品有没有这条路」——grep 下来 `compact()` 只由用户按钮 / 回车触发，没有任何自动路径；② 再问「这个字符串是谁写的」——全仓库只有冒烟装置里那一句在发；③ 最后用**落盘物证**钉死：`out/` 里冒烟产物的时间戳 + 会话 JSONL 里那条 user 消息。**别停在「大概是产品 bug」，也别停在「可能是环境变量」——两边都要证据。** 铁律：测试装置的每一次转发都要先问「真发出去会有什么后果」；断言若只依赖**调用被记录**，就**只记账**，不要转给真实现。副作用清单里最贵的三项是：**计费 / 写用户数据 / 往真实会话里塞假消息**。
  ⑪ **冒烟夹具里的「整份视图」必须钉成契约类型 `ConversationView`**（v1.43 `/skill` 那次，2026-09）：症状：`dock` 新增的 4 条 `/skill` 断言**全红**，报的是「本地拦不住」，看上去像产品坏了。根因：`src/dev/smoke/modes/dock.ts` 里 `viewBase` 造的受控视图会经 `session.view` **整份替换**渲染层手里那份真实视图，而它的返回类型当时写的是 `Record<string, unknown>`——**少写字段编译器不管**；v1.43 给契约新增了 `skills`，这份夹具没跟上，渲染层于是把「本会话技能清单」读成 `undefined`（＝**不知道**，按设计要放行），本地拦截整条失效；同一趟还漏着 `thinkingLevel`（更早就有，只是没人依赖它才一直没暴露）。**「夹具缺字段」和「产品有缺陷」在红字里长得一模一样**。铁律：① 夹具若**整份替换**真实数据，它的类型就**必须是那个契约本体**（这里是 `ConversationView`），覆盖项写 `Partial<契约>`——这样契约再加字段时夹具会**编译不过**，而不是静默把字段抹成 `undefined`；② 一簇新断言**全红**且都指向同一个「没生效」时，先按 §1.2 怀疑**测试接入**（这里是夹具），别先改产品；③ 还要分清两份「视图」：`sessionManager.getView()` 是**主进程**那份状态，渲染层手里那份是**推过去**的——这次前者一直是对的，被盖掉的是后者，只读前者会得出「数据明明是好的」这种**必然误导**的结论。
  ⑫ **探针别靠「层级」猜元素：给被测结构一个它自己的标记**（v1.44 `/` 候选浮层那次，2026-09）：症状：给 `textarea` 套了一层定位容器之后，2 条附件断言变红（报「输入卡片内没找到提示」），而另 1 条——`model/no-usable` 的「输入卡片完整落在窗口内」——**没红，但它已经在量别的东西了**。根因：两处探针都把「`textarea.parentElement`」当成输入卡片（那曾是实测结构）。套上容器之后 `parentElement` 变成了那层容器：前两处的 `card.querySelector("[data-conv-attach-notice]")` 自然找不到提示；后一处则改为去量一层**恒在卡片内部**的元素——它的矩形永远落在卡片之内，那条断言**从此必然为真**（真被 `overflow-hidden` 裁掉也照样过）。**一条必然为真的断言比一条红断言贵得多**：它不会引起任何人注意。铁律：① 探针要认的如果是「某一类界面区域」，就用**那个区域自己的标记**（`[data-conv-card]`、`[data-conv-error]`、`data-dock-tab` 都行），别用 `parentElement` / `children[i]` / 靠 class 碰运气；② 改动一个元素的**祖先链**时，顺手 `grep` 一遍全靠结构认它的探针（本例 grep `parentElement` 就两处，一下就找齐）；③ 一簇断言变红时先分清哪些是**真坏**、哪些是**探针量错了地方**——本例那 2 条红是探针错（产品是好的），而那 1 条**没红**的才是真隐患（同 §1.2）。
  ⑬ **用例的「环境前提」必须显式建立，而且只写一次**（修 `model` 冒烟那次，2026-09）：症状：`model` 模式从 **34/34 掉到 30/34**，红的是 `[model/fallback]` / `[model/keyless]` 各 2 条「默认解析落到…」——看上去像模型解析坏了。根因：这三段（含**没红**的 `[model/no-usable]`）假定的都是「这台机器上**只有**用例造的那个服务」，而默认解析 `resolveSessionModel` 是从**整份** provider 列表里挑的；其中**只有一段**把这个前提显式建立起来了（先把真实服务挪开、`finally` 里还回去），另两段只挪得动内置 `deepseek`——于是本机自己配的 `glm`（带密钥）抢走了默认解析。**同一类前提写了两遍、漏了一段，是这类问题的标准形态**；而没漏的那些段还都是绿的，更容易让人以为「这个套路大家都懂」。铁律：① 凡是假定「机器上只有 X」的用例，都要**显式把别的挪开**，别指望用户机器是干净的；② 同一类前提**抽成一个函数**（这里是 `stashUsableProviders()`），让「漏掉一段」在结构上不可能；③ 挪用户的真实配置要**挑副作用最小的做法**——能删密钥就别删条目（硬崩也只丢一个密钥值），能不删就别删，并保证 `finally` 里**先还密钥、再写回条目**；④ 判「是不是产品坏了」要看那条断言**依赖什么**：这次它只依赖 `resolveSessionModel(…, listProviders())` 与库里那唯一一行 `glm`，所以是环境而不是代码。
  ⑭ **装故障注入的「假 worker」要按归属认人——它是全局的，会撞上用户的真实会话**（v1.62 `rm-workspace` 那次，2026-09）：症状：为了让 `sessionManager.isRunning()` 为真（它的唯一真源是 worker 发来的 `view.running`，**没有任何测试钩子**能直接置位），给 `COLT_WORKER_OVERRIDE` 写了个「发 ready 后立刻声明我在跑」的假 worker——它按设计工作、断言全绿，**看不出任何异常**。但 `COLT_WORKER_OVERRIDE` 是**全局**的（一开，所有会话都 fork 这个脚本），而渲染层挂载时会**自动打开当前项目的第一条会话**，那多半就是**用户的真实会话**：假 worker 一旦被 fork 到它，`ready` 会把假的 `kernelSessionId` 写进那条会话（`setKernelSessionId`，之后它按这名字找 JSONL 会找不到），`view` 会经 `touchSession` 把假的标题与消息数写进去——**往用户数据里塞假消息**。根因：**故障注入脚本没有「归属」概念，而我把「这是谁的会话」当成了理所当然**。铁律：① 假 worker 收到 `init` 先看 `cwd` 里有没有**归属标记**（用例自己的靶子目录名里带一串），不属于自己的**一条消息都不发**（`process.exit`——让主进程快速失败，而不是干等就绪超时）；`ready`/`view` **各有一次真实写入**，发出去就是污染；② **这类污染不能靠「事后查库看起来干净」判定**：假标题会被后来真 worker 按真实历史**改回去**（下一次不带注入的运行就把标题恢复了），**证据消失、库一片干净**——只能看**机制**（谁在什么条件下会写、那个条件此刻成不成立）；③ 用例里配一段**不含归属标记**的靶子，把「写别人数据」那条路**故意走一遍**，断言「open 失败 + 靶子一字未动」——这条才让门禁是**被验过的**，而不是「信我」；④ 「假 worker 被 fork 到了别人的会话」本身可观测：主进程会打「会话进程在就绪前退出」——**看到它就问一句「它刚才想去碰谁的会话」**。
- **本机开着「智能应用控制」（SAC），会拦未签名的 `electron.exe` → `npm run dev` 起不来。**（2026-09 实测）症状：`electron-vite` 报 `Error: spawn UNKNOWN`（errno **-4094**），而 `npm run build` / `npm test` 全绿（它们不拉起 Electron）；PowerShell 直跑那个 exe 会说「**应用程序控制策略已阻止此文件**」。判据：

  ```powershell
  & .\node_modules\electron\dist\electron.exe --version   # 报「已阻止」= 被策略拦
  (Get-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\CI\Policy").VerifiedAndReputablePolicyState
  # 1 = 强制（0 关 / 2 评估）
  ```

  **根因不是签名，是信誉**：官方 Electron 的 `electron.exe` **本来就不签名**（官方 zip 里抽出那份与我装的逐字节相同，`NotSigned`）；SAC 按微软信誉库放行（`esbuild.exe` 同样未签名却能跑）。实测（同机同目录同抽取方式，都是官方件）：**44.3.0（发布 7 天）✗ / 44.2.0（11 天）✓ / 44.0.0（21 天）✓ / 43.3.0（42 天）✓ / 40.10.2（111 天）✓** → 阈值在 **7~11 天**之间。**停在新版本上就会「今天能跑、过几天又跑不了」**。铁律：① `electron` **精确 pin**，别用 `^`（`^44.2.0` 会解析回被拦的 44.3.0）；② 升 Electron 之后**必须真起一次** `npx electron --version`，别只看 `build` 过没过；③ 报 `spawn UNKNOWN` 时先按上面两条量，**不要**先去改项目代码（与业务代码无关）。

- **冒烟里直连会话 IPC 时，渲染层是并发参与者，不是旁观者**（2026-09，memory 冒烟两轮翻车）。症状：`session.open` 报「会话进程在就绪前退出」（三个 open 一起被拒），或 worker 起来了却带着**项目根的 cwd** 而不是用例传的夹具 cwd。根因是三条叠加，缺一不会发作：① 渲染层挂载即自动选中「当前项目」的 `list[0]` 并以**项目 rootPath** 为 cwd 打开（App.tsx）；② Conversation 卸载即 `session.close`（StrictMode 下挂载→卸载→重挂载）——就绪前的 worker 当场被杀，退出码 0、无任何 stderr，**看起来像神秘崩溃**；⚠️ **2026-09-18 起这一条不成立了**：渲染层不再在卸载时关 worker（切走只是失焦，见 Conversation 卸载处的注释与 `reenter` 的两条新断言）。上面那场翻车的**结论仍然有效**——渲染层依旧是并发参与者，仍要按铁律①处理，只是「它会把 worker 杀掉」这个具体机制没了；③ worker 复用分支只同步模型、**不校验 cwd**——先到者定 cwd，后来者被静默忽略（三个 open 挂在同一个 pending 任务上时，一次死亡全体被拒，报错条数还会误导排查）。铁律：① 冒烟里真实 fork worker 的会话，必须让渲染层「看不到它，或看到时 cwd 恰好一致」——现行做法是**会话建在独立夹具项目下 + 跑前把仓库项目顶回 `project.list[0]`**（最近打开优先），见 `NEXT-PHASE.md` §5 第 5 条（memory 模式）；② 判「worker 为什么死了」先看主进程 `disposeReason`（是 dispose 还是自发退出），别对着 exit 码猜；③ 同族案例：`openMemoryDatabase` 编译全绿、单测全绿，运行时**没有任何调用点**（又是「只有定义、没有调用」）——这类缺口唯一可靠的探针就是把真实链路跑一遍。

- **`ELECTRON_RUN_AS_NODE` 会把 `electron.exe` 变成 Node，而报错看着像产物坏了**（2026-09 实测）。症状：`npm run dev` 报 `SyntaxError: The requested module 'electron' does not provide an export named 'BrowserWindow'`（指向 `out/main/index.js` 第一行那句 import），并打出 `Node.js v24.x`——实际是**根本没起 Electron**，那行 import 是在 Node 的 ESM 加载器里被解析的。判据（一条命令定性）：

  ```bash
  ./node_modules/electron/dist/electron.exe --version
  # 打 v24.x  → 被按成了 Node（环境变量问题）
  # 打 v44.x  → 真的 Electron
  ```

  **坑**：把变量设成**空串不算清掉**——Electron 侧是 C++ `getenv()` 判断，空串照样算「已设置」。必须 **unset**，并顺手去掉环境里挂的 `NODE_OPTIONS`（可能有 `--require` 钩子）：

  ```bash
  env -u ELECTRON_RUN_AS_NODE -u NODE_OPTIONS COLT_SMOKE=dock.png COLT_SMOKE_MODE=dock npm run dev
  ```

  铁律：`--version` 打出 Node 版本号时**先查环境变量**，别去翻 `out/`、也别怀疑打包配置。

- **跑单测要用仓库自己的加载器**：`npx tsx --test tests/x.test.ts` **不认** tsconfig 里的 `@shared/*` 路径别名（会 `ERR_MODULE_NOT_FOUND: Cannot find package '@shared/...'`），看起来很像是「别名写错了 / 文件没了」。用仓库的方式跑：

  ```bash
  node --import ./tests/ts-resolve.mjs --test tests/ask-user.test.ts   # 单个文件
  npm test                                                            # 全量
  ```

- **冒烟产物落在 `out/` 且已被 gitignore**（`out/<名字>.png` + `<名字>.log`）。结论以 `.log` 为准（Windows 上 Electron 主进程 stdout 不接父终端，只看控制台会丢日志）；跑完记得看 `通过 N/M` 那一行，别只看「DONE」。

- **被「后台节流」的内嵌浏览页会自报 hidden，而 hidden 的页面不重排**（2026-09-18 定位并修复）。症状：`dock` 掉 3 条——**全是读「页面自己的 `window.innerWidth`」**的那几条（`覆盖尺寸真的落到页面 … 页面 innerWidth=543，期望 1280`、`右栏拉宽后比例自动重算 … 页面 CSS 视口 365 ≈ 需要宽 700`、`「还原」后回到 100% … 页面 365`），`fixture` 掉 `viewport 窄屏生效`；**主进程侧的断言一律全绿**（原生视图 `getBounds()` 确实成了 1280、与「页面区域」逐像素对齐、缩放比例算得对）——只有**页面侧的布局读数**是冻结的。根因（探针实测，不是推测）：把内嵌页与应用 UI **并排量一次**——同一时刻**应用 UI `visibilityState=visible`、`hasFocus=true`**、主窗口 `visible / 未最小化 / focused`，而**内嵌页 `visibilityState=hidden`、`innerWidth` 冻在 543**；Electron 的 `backgroundThrottling`（默认开）**同时驱动 Page Visibility API**，于是被节流的页面自报 hidden，**而 Chromium 不给 hidden 的页面重排**——视图设了多大都白搭。对照实验（缺了它就会误判成「环境问题」）：空等 1.5s 读数不变，`webContents.setBackgroundThrottling(false)` 一关**立刻**从 543 变 1280。**不是「窗口没在前台」**（加 `window.show()+focus()` 照样红，该假设已证伪），也**不是「无人值守才有的环境噪音」**（同代码的作者绿记录 `out/dock.png.log` 只是碰巧没进节流态）。修法：`browser-host.ts` 的 `#applyBounds` 里**成对**做——可见时 `setBackgroundThrottling(false)`、收起时恢复 `true`（收起时保留节流，免得隐藏页的定时器白烧 CPU）；修完 `dock` 211/211、`fixture` 25/25。铁律：① **「视图摆成多大」和「页面按多大重排」是两件事**——判「重排没重排」只能读**页面自己的** `window.innerWidth`（且必须从**浏览器视图的 webContents** 读），主进程侧全绿**不能**当作页面也对；② 页面被判 hidden 时**先怀疑节流**（`backgroundThrottling` 会连带 Page Visibility API），别一上来去改布局/尺寸代码；③ 看到「主进程都对、只有页面侧不对」，**别急着归因到环境**——这次它长得像环境问题，实际是条件性缺陷（用户拖右栏时页面可能就卡在旧布局）。

- **`app.isPackaged` 会在启动过程中「晚值漂移」，任何运行期求值都可能误判成「已打包」**（2026-09-18 定位并修复）。症状：`crash` 冒烟已经装了故障注入（`COLT_WORKER_OVERRIDE=scripts/crash-worker.cjs`）却打出 `session.open 结果：OK（2ms）`——**看着通过，其实注入根本没生效**。临时探针打出 `packaged=true override="…crash-worker.cjs" path=out\main\worker.js`：worker 被 fork 的那一刻（`ready-to-show`）`app.isPackaged` 已经**从 false 漂成了 true**，于是 `!app.isPackaged` 这道闸门恒为假。这不是崩溃，是**静默失效**——比崩溃更难发现：`build` / `typecheck` / 单测全绿，只是那条路径根本没被走到。修法：抽 `src/main/lib/app-mode.ts` 的 `isDev = process.defaultApp === true || !app.isPackaged`，**在启动早期求值一次、之后一律复用这个常量**；dev 资源加载与 `Colt` / `Colt-dev` 数据目录分流共用它。另一层：`crash` 模式**自己自证前提**——不满足 `isDev && COLT_WORKER_OVERRIDE` 就打印「跳过：未装故障注入…」并返回，而不是交出一份看着通过的日志（修后：装上注入 → `REJECTED … 耗时 107ms`；没装 → 明说跳过）。铁律：① 凡是「运行期才求值」的 `app.isPackaged`，都要怀疑晚值漂移——**要用就在启动早期定格**；② 故障注入类用例必须**先自证「注入真的装上了」**，否则它验的是一个不存在的场景；③ 「用例通过」与「被测路径被走到」是两回事，前者不蕴含后者。

- **`session.open` 的 `cwd` 是必填，而 `JSON.stringify` 会静默抹掉 `undefined`**（2026-09-18，`advanced` 冒烟）。症状：`advanced` 报 `Cannot read properties of undefined (reading 'startsWith')`，栈落在 `resolvePath` ← `NodeExecutionEnv.absolutePath` ← `JsonlSessionRepo.resolveCreateDestination`——**报错现场与真实原因毫无关系**，看着像内核坏了。根因：用例写的是 `cwd: process.env.COLT_SMOKE_CWD`（没设就是 `undefined`），`JSON.stringify` 把整个键丢掉，主进程照契约收下这份「缺 cwd」的请求，一路带到内核做路径解析才炸。铁律：① 冒烟里 `cwd` 一律写 `process.env.COLT_SMOKE_CWD ?? process.cwd()`，别裸传环境变量（`advanced` / `approval` / `crash` / `reenter` / `host` 已统一）；② 见到「与现场无关的 `undefined.xxx`」先怀疑**某个必填入参在序列化时没了**；③ 这与上一条同源——都是「用例的环境前提没显式建立」（见本节 ⑬）。

- **`approval` 冒烟的场景三/四，前提是「模型会选用 edit/write」——而选工具的是模型，不是用例**（2026-09-18）。症状：日志打出 `文件改动：0 项（预期 >=1）`，看着像「批准后文件没改」的产品缺陷。根因：模型没听「用 edit 工具」，改用 `bash … 2>/dev/null`（含重定向，**照样被正确拦下**，风险分级本身是好的），于是被批准的其实是个 `ls/find`——「批准后文件真的改了」这条前提根本没成立。修法：先在被拦条目里找 `edit`/`write`，找不到就**明说「前提未成立，跳过」**并打出工具轨迹，而不是照打一条假红；场景四的 `running` 也改成**轮询等它落地**——模型被拒后可能接着去调 `ask_user`（在**等用户作答**、`running` 本就该是 true），固定 sleep 读一次必然打成假红。铁律：断言要挂在「用例自己能定的前提」上；前提由外部（这里是模型）决定时，要么显式建立、要么**明说没建立**，不要让它默认成立。
